export default `# 反向代理、限流与 CDN

> **反向代理**部署在服务器一侧：客户端访问统一入口，由代理转发至后端服务。  
> 常与 **负载均衡、TLS 终止、限流、CDN** 一起出现在工程入口层。  
> **学会之后**：能对照正向/反向代理，并说明 CDN 与健康检查直觉。

## 学会之后（验收）


\`\`\`term
{"title":"反代探活（形态演示）","prompt":"$ ","steps":[{"type":"in","text":"curl -I https://example.com"},{"type":"out","text":"HTTP/2 200\\nserver: nginx\\ncontent-type: text/html"},{"type":"in","text":"curl -sS https://example.com/api/health"},{"type":"out","text":"{\\"success\\":true,\\"message\\":\\"ok\\"}"}]}
\`\`\`


| 能力 | 成功信号 |
|------|----------|
| 反向代理 | 靠近服务端的统一入口 |
| 正向代理 | 靠近客户端（Clash 线） |
| CDN | 副本靠近用户，不是另一种 HTTP |
| 上线 | 健康检查、限流、只暴露 80/443 |

## 本课分块

| 块 | 目标 |
|----|------|
| **正向 vs 反向代理** | 位置与谁感知代理 |
| **反向代理能力** | LB、TLS、缓存、防护 |
| **CDN** | 边缘缓存与源站关系 |
| **Cloudflare 直觉** | 托管 DNS + 全球反代 |
| **限流** | 控制请求速率 |
| **八股 × 业务** | L4/L7 LB、会话保持、健康检查 |
| **与本仓** | www 挂载与网关直觉 |

---

## 1. 正向代理 vs 反向代理

\`\`\`match
{"title":"反代与正向代理配对","pairs":[{"id":"fwd","left":"正向代理","right":"靠近客户端；常需配置才能出网（如 Clash）"},{"id":"rev","left":"反向代理","right":"靠近服务器；客户端通常无感（如 Nginx）"},{"id":"lb","left":"负载均衡","right":"把请求分到多台后端"},{"id":"502","left":"502 Bad Gateway","right":"网关后面的上游出问题"}]}
\`\`\`

\`\`\`decide
{"title":"502 了先怪谁？","start":"s","caption":"先分清后端挂了还是门面配错了。","steps":[{"id":"s","q":"浏览器直连后端端口正常吗？","options":[{"label":"后端也不通","next":"be"},{"label":"后端通，经域名/反代才 502","next":"rp"}]},{"id":"be","result":"先修后端进程、端口监听与防火墙。","detail":"反代只是门面；上游没人听就会 502。"},{"id":"rp","result":"查反代 upstream、健康检查、超时与 TLS。","detail":"502 = 网关后面的上游出问题。"}]}
\`\`\`

\`\`\`fill
{"title":"探活命令补全","caption":"只看响应头，不下载整页。","template":"curl -___ https://example.com","answers":["I"],"hint":"大写 i：只取响应头。","caseSensitive":true}
\`\`\`


| | 正向代理 | 反向代理 |
|--|----------|----------|
| 位置 | 靠近客户端 | 靠近服务器 |
| 客户端是否配置 | 常需配置 | 通常无感 |
| 用途 | 访问控制、出海、缓存 | 负载均衡、TLS、缓存、防护 |
| 例子 | 浏览器代理、Clash | Nginx、Caddy、云负载均衡 |

---

## 2. 反向代理常见能力

1. **负载均衡**：分发到多台后端  
2. **TLS 终止**：入口统一证书  
3. **缓存**：减轻源站  
4. **安全**：隐藏源站；可配合 WAF / IP 限制  
5. **健康检查**：摘除故障实例  

\`\`\`
客户端 → 反向代理 → 后端 1
                 ↘ 后端 2
\`\`\`

开发时「同源代理到 API」亦属反代思路，用于规避浏览器跨域限制。  
上游异常时常见 \`502\` / \`504\`。

---

## 3. CDN（内容分发网络）

**CDN**：把静态资源（及部分动态加速）缓存在靠近用户的边缘节点。

| 对比 | CDN 边缘 | 源站反向代理 |
|------|----------|--------------|
| 主要目标 | 就近命中、减延迟与带宽 | 应用入口、路由与防护 |
| 典型内容 | JS/CSS/图片/视频分片 | API、SSR、动态请求 |
| 未命中时 | 回源到源站（或中层） | 直接打后端 |

记忆：CDN 解决「拷贝放到用户旁边」；反代解决「门口如何接待与分发」。二者可叠加。

### Cloudflare 一类产品（预告）

把 **权威 DNS + 可选全球反代（橙云）+ CDN + WAF** 做成托管服务：访客先到边缘 PoP，再按需回源。  
细节（IP 池、地域、路径过滤症状）见下一课 **边缘与出口实务**。

---

## 4. 限流

控制单位时间请求量，保护后端并抑制滥用。

| 算法 | 直觉 |
|------|------|
| 固定窗口 | 每分钟最多 N 次 |
| 滑动窗口 | 最近一段时间合计 ≤ N |
| 令牌桶 | 允许一定突发 |
| 漏桶 | 匀速流出 |

落地：Nginx \`limit_req\`、Redis 计数、网关中间件等。

\`\`\`quiz
{"title":"反代 / CDN 自测","questions":[{"q":"正向代理 vs 反向代理？","choices":[{"t":"正向靠近客户端常需配置；反向靠近服务器客户端通常无感","ok":true,"why":"Clash 偏正向；Nginx 门面偏反向。"},{"t":"二者完全等价","ok":false,"why":"位置与谁感知不同。"},{"t":"反向代理废除 DNS","ok":false,"why":"仍要解析到入口。"},{"t":"正向专门签发证书","ok":false,"why":"证书常在反代/边缘终止。"}]},{"q":"L7 相对 L4 负载均衡多了什么？","choices":[{"t":"能按 Host/URL/Header 等应用语义路由","ok":true,"why":"L4 只看 IP/端口/协议。"},{"t":"禁止用于生产","ok":false,"why":"差异在功能不是禁令。"},{"t":"只能看 MAC","ok":false,"why":"MAC 是链路层。"},{"t":"只能跑 UDP","ok":false,"why":"HTTP L7 常见于 TCP/QUIC。"}]}]}
\`\`\`

---

## 5. 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **L4 负载均衡（Layer 4 Load Balancing）** | 在传输层按 IP、端口、协议转发连接，不解析 HTTP 内容。 | 云 NLB、LVS 把 TCP 443 分到多台机器；适合 TLS 透传、数据库代理、游戏端口。 | L4 LB ≠ 「四层就比七层快很多」；差异在功能，不在绝对速度。 |
| **L7 负载均衡（Layer 7 Load Balancing）** | 在应用层理解 HTTP/gRPC 等，能按 Host、URL、Header 路由。 | Nginx 把 \`/api\` 转到 Node、静态页转到 OSS；本仓 \`mountCoreWwwStatic\` 按路径挂多 Core 是同类门面思路。 | L7 不是只能 HTTP 文本；gRPC、WebSocket 也可 L7，但要能解析协议。 |
| **轮询 / 加权 / 最少连接（Round Robin / Weighted / Least Connections）** | 常见分发算法：依次发、按权重比例发、优先发给当前连接数最少的实例。 | 机器性能不一用 weighted；长连接 WebSocket 场景常用 least_conn 避免全堆一台。 | 轮询 ≠ 会话保持；纯轮询可能把同一用户打到不同后端。 |
| **会话保持 / 粘滞（Session Affinity / Sticky Session）** | 尽量让同一客户端的请求始终落到同一后端实例。 | 后端存本地 Session 未共享时需要；更好方案是把 Session 放 Redis，就不需要粘滞。 | 粘滞 ≠ 负载均衡坏了；它是刻意策略，但扩缩容时可能失衡。 |
| **健康检查（Health Check）** | 负载均衡或编排系统定期探测后端是否存活，失败则摘除。 | K8s \`livenessProbe\`、Nginx \`max_fails\`、云 LB 调 \`/health\`；实例假活会被自动下线。 | 健康检查通过 ≠ 业务逻辑正确；只说明端口/探针路径能响应。 |
| **TLS 终止 / 透传（TLS Termination / Passthrough）** | 终止：在反代处解密 HTTPS，后端走 HTTP 或再加密；透传：加密流量原样转给后端解密。 | 门面统一证书省后端配置是终止；后端要自己验客户端证书或合规要求端到端加密时用透传。 | TLS 终止 ≠ 「不安全」；内网段仍可用 mTLS 保护反代到后端。 |
| **蓝绿 / 金丝雀 / 灰度（Blue-Green / Canary / Gray Release）** | 蓝绿：两套环境一键切换；金丝雀：先给少量流量新版本；灰度：按规则逐步放量。 | 网关按 Header、用户 ID、百分比把 5% 流量打到 v2；观察错误率再全量。 | 灰度 ≠ A/B 测试产品方案；灰度侧重发布风险，A/B 侧重业务指标。 |
| **回源 / 回源鉴权（Origin Fetch / Origin Authentication）** | CDN 缓存未命中时向源站拉内容；回源鉴权确保只有边缘节点能拉源。 | 源站安全组只放行 CDN 回源 IP 段；回源 Host 头要与源站虚拟主机配置一致，否则 404。 | 回源 ≠ 浏览器直连源站；用户打的是 CDN，CDN 再打源。 |
| **令牌桶 / 漏桶（Token Bucket / Leaky Bucket）** | 两种经典限流算法：桶里攒令牌按速率消耗，或固定速率漏出请求。 | API 网关对开放平台限 QPS、防刷；突发流量令牌桶允许一定 burst，漏桶更平滑。 | 限流 ≠ 熔断；限流是主动拒超额，熔断是下游故障时快速失败。 |

业务串联：用户 → CDN/LB → 反代 → 多实例；\`502\` 查上游与健康检查；登录态乱跳查粘滞或改 Redis Session。

---

## 6. 与本仓

主服将多个 Core 的 \`www/<应用>/\` 挂到同一入口，直觉接近「按路径分流的门面」。  
生产环境常在更外层再加云 LB / CDN / TLS。

## 下一步

**Nginx** — 把反代概念落到具体产品；  
**边缘与出口实务**（IP 池、Cloudflare、地域与代理）；第四章 **HTTP 与 www**；  
番外 **代理引擎**（正向代理侧对照）；需要打包中间件 → 番外 **容器**。
## 导图2 · CDN / HTTPS / 重定向 × 反代与负载

> 导图2 的 CDN/HTTPS 是上线高频词；本课钉 **反向代理、L4/L7、健康检查、粘滞**。  
> **正向代理（Clash）在番外**——别和门面反代混层。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **CDN** | 副本放到近处 | 缓存未命中才回源；与反代可串联，职责不同 |
| **HTTPS** | 入口加密 | TLS 终止 vs 透传；证书常落在门面 |
| **重定向** | 换 URL | 门面可做 301/302；业务重定向与网关要分清谁发 |
| **部署上线** | 多实例可访问 | LB 算法、健康检查、蓝绿/金丝雀在入口层 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
