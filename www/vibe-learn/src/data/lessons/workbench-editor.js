/** 工作台 · 编辑器 */
export default `# 工作台 · 编辑器

> 终端会发令，还需要一个**改文件的地方**。可用 VS Code、**Cursor**、**Trae** 等；本课先定习惯，品牌见番外 **AI 编程工具**。

## 本课你要带走

1. 用编辑器打开**仓库根目录**（不是随便一个子文件夹瞎开）
2. 内置终端与系统终端是同一类 Shell
3. 保存 → 再跑命令（没保存等于没改）

---

## 1. 打开正确的文件夹

打开 \`XRK-AGT\` 仓库根，以便：

- 看到 \`package.json\`、\`core/\`、\`src/\`
- 终端默认 cwd 在根，\`pnpm i\` / \`node app\` 路径才对

\`\`\`flip
{"title":"工作台翻卡","cards":[{"front":"工作区根","back":"含 package.json 的仓库根"},{"front":"单文件乱开","back":"相对路径、Git、终端 cwd 全容易错"},{"front":"内置终端","back":"与系统终端同类；注意选 Git Bash / pwsh"}]}
\`\`\`

---

## 2. 最小插件建议

| 用途 | 说明 |
|------|------|
| 资源管理器 | 浏览 \`core/\` |
| 搜索 | 全局找符号 / 报错字符串 |
| 终端 | 跑 \`node\` / \`pnpm\` |
| Git | 看 diff（提交前自检） |
| AI Agent（若用 Cursor 等） | 先 Ask/小步；规则见 **AI 编程工具** |

---

## 3. 和第一章的关系

编辑器改磁盘上的文件 → Shell 起进程读这些文件。  
**PATH / Node / pnpm** 仍由第一章保证；编辑器不替代运行时。

## 下一步

**分层排障** — 坏了先问「落在哪一层」。  
可选：**AI 编程工具 · 形态地图**；卡住时用 **Vibe Coding 心智** 里的提问模板。
`;
