export default `# 工作台 · 编辑器

> 终端会发令，还需要一个**改文件的地方**。可用 VS Code、**Cursor**、**Trae** 等；本课定习惯，品牌细节见番外 **形态与黄页** / **Vibe Coding 心智**。  
> **学会之后**：打开仓库根、内置终端选对壳、保存后再跑命令、会用搜索与 Git diff 验收改动。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 仓库根 | 用编辑器打开含 package.json 的根 |
| 终端 | 内置终端与系统壳一致要对齐 cwd |
| diff | 会看改动再保存/提交 |
| 跟 Agent | 说明编辑器与打开的根路径 |

## 本课你要带走

1. 用编辑器打开**仓库根目录**（含 \`package.json\` / \`pnpm-lock.yaml\` 的那一层）  
2. 内置终端与系统终端是同一类 Shell（Windows 优先 **Git Bash** 或 **pwsh**，别混用方言抄命令）  
3. **先保存再跑命令**（没保存 = 磁盘上还是旧文件；进程读的是磁盘）  
4. 会看 **Git diff**、用搜索定位报错字符串  
5. 改 \`core/<产品>/\` 时别误伤 \`src/infrastructure\`（基础设施与业务分层）

---

## 1. 打开正确的文件夹

打开 \`XRK-AGT\` 仓库根，以便：

- 看到 \`package.json\`、\`core/\`、\`src/\`、\`pnpm-lock.yaml\`、\`docs/\`  
- 终端默认 cwd 在根，\`pnpm i\` / \`node app.js\` 路径才对  
- 多根工作区乱开子目录 → 相对路径、Git、Agent 交底全容易偏  

| 错误打开方式 | 常见后果 |
|--------------|----------|
| 只开 \`core/某插件\` | \`pnpm\` 找不到根清单；Agent 以为仓库就这么大 |
| 只开单个 \`.js\` | 无工作区根；搜索/Git/终端 cwd 全飘 |
| 开到上一级杂货目录 | 误操作别的项目；锁文件对不上 |

\`\`\`flip
{"title":"工作台翻卡","cards":[{"front":"工作区根","back":"含 package.json 的仓库根"},{"front":"单文件乱开","back":"相对路径、Git、终端 cwd 全容易错"},{"front":"内置终端","back":"与系统终端同类；注意选 Git Bash / pwsh"},{"front":"未保存就跑","back":"进程读的是磁盘旧内容"}]}
\`\`\`

**自检（30 秒）：** 编辑器资源管理器顶层应能直接看到 \`package.json\` 与 \`core/\`；内置终端执行 \`pwd\`（或 \`Get-Location\`）应落在该根。

---

## 2. 最小面板习惯

| 用途 | 你在干什么 | 第一章对应 |
|------|------------|------------|
| 资源管理器 | 逛 \`core/<产品>/\`，业务放 Core；基础设施在 \`src/\` | 第四章鸟瞰会再钉 |
| 搜索 | 全局找符号 / 报错原文（整段粘贴进 Agent） | 分层排障 |
| 终端 | \`node\` / \`pnpm\` / \`git\`；与系统终端同一 PATH | PATH · 包管理器 |
| 源代码管理 | 提交前看 **diff**；Accept Agent 改动也靠它 | Git 工作区 |
| AI 面板 | 先 Plan/小步；Rules 见 **项目记忆文件** | Vibe Coding |

快捷心智（各产品键位不同，记概念即可）：**保存 · 全局搜索 · 切换终端 · 命令面板**。

### 内置终端选型（Windows）

| 壳 | 适合 | 注意 |
|----|------|------|
| **Git Bash** | 跟 Linux/文档命令最省心 | 与 Claude Code / 多数教程方言一致 |
| **PowerShell** | 系统管理、Windows 原生 | 别把 \`ls -la\` 当万能；对象管道另一套 |
| **CMD** | 遗留 | 尽量别当主工作壳 |

装 Node / 改 PATH 后：**关掉终端标签再开**，否则仍是旧环境块。

---

## 3. 和第一章 / Agent 的关系

\`\`\`mermaid
flowchart LR
  ED[编辑器改文件] --> DISK[磁盘]
  SH[Shell 起进程] --> DISK
  AG[Coding Agent] --> ED
  AG --> SH
\`\`\`

- **PATH / Node / pnpm** 仍由第一章保证；编辑器不替代运行时  
- Agent 改文件后：**你**审 diff → 保存 → 本机再跑（\`pnpm\` / \`node app.js\`）  
- 卡住：用 **分层排障** + **Vibe Coding 心智** 提问四件套（目标·现场·约束·验收）  

\`\`\`quiz
{"title":"编辑器","questions":[{"q":"只打开 core/某插件单文件当工作区，最容易？","choices":[{"t":"更快，推荐","ok":false,"why":"丢根目录上下文。"},{"t":"终端 cwd / Git / 搜仓全别扭","ok":true,"why":"应打开仓库根。"},{"t":"与打开根完全等价","ok":false,"why":"不等价。"}]},{"q":"Agent 改完文件你立刻 node app，但行为没变，优先怀疑？","choices":[{"t":"内核坏了","ok":false,"why":"过激。"},{"t":"文件未保存或 cwd 不在仓库根","ok":true,"why":"进程读磁盘；目录错则跑到别的树。"},{"t":"必须改成 npm","ok":false,"why":"本仓仅 pnpm。"}]}]}
\`\`\`

## Coding Agent 协作

\`\`\`prompt
目标：确认我已在正确的仓库根工作，并选对内置终端壳。
现场：编辑器打开的顶层文件夹=…；终端 pwd=…；node -v=…；pnpm -v=…
约束：不要改业务代码；只纠正工作区/终端配置。
验收：列出「已正确」或「还差哪一步」；给出下一道可复制命令。
\`\`\`

## 下一步

**分层排障** — 坏了先问「落在哪一层」。  
然后 **首次跑通**；可选：**Vibe Coding 心智**。
## 导图2 · 终端 / Git Diff / 前端 × 工作台

> 导图2 较少谈 IDE；本课钉工作台打开方式。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **终端命令行** | 内置终端 | cwd 必须是仓库根 |
| **Git Diff** | 看改了什么 | Accept Agent 前必看 |
| **前端** | www 项目可在此打开 | 仍是 Core 内路径 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
