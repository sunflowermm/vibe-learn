export default `# 工作台 · 编辑器

> 终端发令；编辑器改文件。可用 VS Code / Cursor / Trae 等——习惯相同。  
> 品牌细节见番外 **Vibe Coding 心智**。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 仓库根 | 打开含 \`package.json\` 的根 |
| 终端 | 内置终端 cwd 与壳方言正确 |
| 磁盘 | 先保存再跑命令 |
| 跟 Agent | 说明编辑器与打开的根路径 |

---

## 1. 打开正确的文件夹

顶层应直接看到 \`package.json\`、\`core/\`、\`src/\`。\`pwd\` 落在该根。

| 错误打开 | 后果 |
|----------|------|
| 只开 \`core/某插件\` | 找不到根清单；Agent 上下文偏 |
| 只开单个 \`.js\` | 无工作区；搜索 / Git / cwd 全飘 |
| 开到上一级杂货目录 | 误操作别的项目 |

---

## 2. 最小习惯

| 面板 | 干什么 |
|------|--------|
| 资源管理器 | 逛 \`core/<产品>/\`；别误伤 \`src/infrastructure\` |
| 搜索 | 用报错原文定位 |
| 终端 | 与系统同一 PATH；Windows 优先 Git Bash / pwsh |
| 源代码管理 | 审 **diff** 再提交 / Accept Agent |

改 PATH 后：**关掉终端标签再开**。

\`\`\`mermaid
flowchart LR
  ED[编辑器改文件] --> DISK[磁盘]
  SH[Shell 起进程] --> DISK
  AG[Agent] --> ED
  AG --> SH
\`\`\`

Agent 改完：**你**审 diff → 保存 → 本机再跑。

\`\`\`prompt
目标：确认已在正确仓库根，并选对内置终端壳。
现场：顶层文件夹=…；终端 pwd=…；node -v=…；pnpm -v=…
约束：不改业务代码。
验收：列出已正确或还差哪一步。
\`\`\`

## 下一步

**分层排障** · **首次跑通**
`;
