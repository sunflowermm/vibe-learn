export default `# 番外 · 代理引擎（Clash 线）

> 把第三章的网络词汇，接到你电脑上那台「迷你路由器」——**代理引擎**。  
> 本框三张卡片成一条短链：**引擎本质 → 端口与监听 → 订阅与配置**。  
> **章专属动画**：\`proxyroute\`（应用 → 本机引擎 → DIRECT/节点 → 目标）。  
> 与第三章 **反向代理 / CDN / 边缘** 对侧：那边是访客进站门面；这边是你出门选路。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 正向代理 | 分清本机选路 vs 第三章反代门面 |
| 环境变量 | 知道何时设 \`HTTP_PROXY\`，本机/国内何时直连 |
| 边界 | 不把 Clash 当成 Nginx 或容器 |

## 零基础先建立画面

你没有在「改互联网」，你只是在本机多跑了一个程序：

1. 它 **listen** 某个端口（如 7890 / mixed-port）  
2. 浏览器 / Agent **连到这个端口**  
3. 它按规则决定：**直连** 还是 **经节点转发**  

和家用路由器很像，只是舞台缩到了你的电脑里。

\`\`\`algo
{"kind":"proxyroute","title":"番外总览 · 正向选路","autoplay":true,"speed":880,"data":{"mode":"proxy","dest":"境外资源","rule":"rules → PROXY 组"}}
\`\`\`

## 知识串（必须先有的词）

| 正章概念 | 番外里长什么样 |
|----------|----------------|
| **正向代理** | 引擎更靠近你，替你出门 |
| **端口 / listen** | 引擎在本机监听，程序连过来才算走代理 |
| **路由** | 规则表 ≈ 应用层选路（DIRECT / PROXY） |
| **DNS** | 名字解析对不对，直接影响规则是否命中 |
| **节点** | 远端跳板；订阅用来更新节点列表 |
| **环境变量** | \`HTTP_PROXY\` / \`HTTPS_PROXY\` / \`NO_PROXY\`（第二课） |
| **边缘出口** | 走节点后对端看到节点出口 IP（对照边缘实务） |

\`\`\`mermaid
flowchart LR
  A[网络正章] --> B[引擎本质]
  B --> C[本机端口与 HTTP_PROXY]
  C --> D[订阅与配置]
  E[边缘实务] -.->|对侧| B
\`\`\`

## 建议阅读顺序

1. **引擎本质** — 站在协议栈哪一侧；\`proxyroute\` 看选路  
2. **端口与 Coding Agent** — 系统代理 vs \`HTTP_PROXY\` 族  
3. **客户端配置** — 订阅、节点、系统代理最小路径  

## 为何单独成框

用工程副线巩固网络主线：同一套名词，换一个你更可能亲手点开的软件。  
合规：本框只讲机制与排障；跨境访问须遵守当地法规与服务条款。

## 入口

从 **网络是什么** / **边缘实务** 伸入即可；环境变量地基见第一章 **安装器与 PATH**。

## 下一步

打通后进第四章 **部署环境**（clone GitHub）；  
需要门面侧对照回 **Nginx / 反向代理**。
`;
