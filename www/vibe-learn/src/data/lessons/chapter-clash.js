/** 番外章说明 · Clash */
export default `# 番外 · 代理引擎（Clash 线）

> 把第三章的网络词汇，接到你电脑上那台「迷你路由器」——**代理引擎**。  
> 本框三张卡片成一条短链：**引擎本质 → 端口与监听 → 订阅与配置**。  
> 每课末有 **八股 × 业务串联**：四列表展开 TUN、SOCKS5、NO_PROXY、GEOIP 等，和正章 SNAT/反代对照着记。

## 零基础先建立画面
\`\`\`steps
{"title":"代理番外路径","steps":[{"title":"引擎是什么","body":"本机端口服务。"},{"title":"端口与变量","body":"系统代理 ≠ HTTP(S)_PROXY。"},{"title":"上手","body":"订阅 → 节点 → 系统代理。"}]}
\`\`\`


你没有在「改互联网」，你只是在本机多跑了一个程序：

1. 它 **listen** 某个端口（如 7890）  
2. 浏览器 / Agent **连到这个端口**  
3. 它按规则决定：**直连** 还是 **经节点转发**  

和家用路由器很像，只是舞台缩到了你的电脑里。

## 知识串（必须先有的词）

| 正章概念 | 番外里长什么样 |
|----------|----------------|
| **正向代理** | 引擎更靠近你，替你出门 |
| **端口 / listen** | 引擎在本机监听，程序连过来才算走代理 |
| **路由** | 规则表 ≈ 应用层选路（DIRECT / PROXY） |
| **DNS** | 名字解析对不对，直接影响规则是否命中 |
| **节点** | 远端跳板；订阅用来更新节点列表 |
| **环境变量** | \`HTTP_PROXY\` / \`HTTPS_PROXY\` / \`NO_PROXY\`：终端与 Agent 常靠它们认代理（见第二课） |

\`\`\`mermaid
flowchart LR
  A[网络正章] --> B[引擎本质]
  B --> C[本机端口与 HTTP_PROXY]
  C --> D[订阅与配置]
\`\`\`

## 建议阅读顺序

1. **引擎本质** — 它在协议栈里站哪一侧  
2. **端口与 Coding Agent** — 系统代理 vs \`HTTP_PROXY\` 族；为何浏览器通了终端却不通  
3. **客户端配置** — 订阅、节点、系统代理最小路径  

## 为何单独成框

它不是 XRK 正章，而是用工程副线巩固网络主线：同一套名词，换一个你更可能亲手点开的软件。

## 入口

从 **网络是什么** 伸入即可；端口与 \`HTTP_PROXY\` 在短链第二课；环境变量地基见第一章 **安装器与 PATH**。

## 结合知识导图2

| 本框 | 导图2 | 钉死 |
|------|-------|------|
| 本机监听端口 | **端口** · **HTTP** · **URL** | 代理引擎是本机服务，不是「改了整个互联网」 |
| \`HTTP_PROXY\` 族 | **环境变量** · **终端** | 浏览器通 ≠ 终端 Agent 通 |
| 正向代理选路 | 与第三章 **HTTP / DNS** 同一套词 | 正向代理 ≠ 反向代理（网关） |

跨导图查「端口 / 环境变量」；规则与订阅仍以本框三课为准。

`;
