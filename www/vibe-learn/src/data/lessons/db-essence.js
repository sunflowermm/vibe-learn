/** 数据库本质 */
export default `# 数据库本质

> **数据**是要保存与查询的信息；**数据库**常指「按某种模型组织好的数据集合」；  
> **DBMS（数据库管理系统）**是管理这些数据的软件（建表/文档、查询、并发、持久化、权限）。  
> 口语里说「上了个 MySQL / Redis」，多半指 **DBMS 产品 + 跑起来的服务实例**。  
> 分层对照（语言/运行时/框架）已在语言章钉死；本课只钉 **数据这一层**。

## 本课分块

| 块 | 目标 |
|----|------|
| **三层名词** | 数据 · 数据库 · DBMS |
| **解决什么问题** | 为何不直接写文件 |
| **数据模型** | 关系 / 文档 / 键值 / … |
| **不是什么** | ≠ 语言 ≠ 框架 ≠ 运行时 |

---

## 1. 三层别混

| 词 | 含义 | 例子 |
|----|------|------|
| **数据** | 业务要记住的事实 | 用户 id、订单金额 |
| **数据库** | 按模型组织的数据集合 | 某个库名 \`agt\`、某组 key 空间 |
| **DBMS** | 管理软件 | PostgreSQL、MongoDB、Redis、SQLite |

---

## 2. 为什么需要 DBMS（本质动机）

只把 JSON 丢进磁盘文件，很快会撞上：

| 问题 | DBMS 常提供的能力 |
|------|-------------------|
| 并发读写互相踩 | 锁、MVCC、事务 |
| 查得慢 | 索引、执行计划 |
| 进程挂了丢数据 | 持久化、WAL、备份 |
| 多机多应用共享 | 网络协议、权限、连接管理 |
| 结构演变 | Schema / 迁移（因产品而异） |

**一句话：** DBMS 把「可靠地存、查、改共享数据」做成可复用的系统能力。

---

## 3. 常见数据模型（先认门派）

| 模型 | 直觉 | 代表 |
|------|------|------|
| **关系（表）** | 行列表格 + SQL | PostgreSQL、MySQL、SQLite、Oracle、SQL Server |
| **文档** | JSON/BSON 文档集合 | **MongoDB** |
| **键值 / 结构** | key → 字符串/哈希/列表… | **Redis** |
| **搜索倒排** | 全文检索 | Elasticsearch、OpenSearch |
| **向量** | 嵌入相似度检索 | 各向量库 / PG 扩展等 |

同一产品也可 **多模型**（例如 PostgreSQL 有 JSON/向量扩展）。选型看访问模式，不是看名字酷不酷。

---

## 4. 明确：数据库不是这些东西

| 不是 | 那是什么 | 对照 |
|------|----------|------|
| **编程语言** | JS/Python/SQL 方言另说 | SQL 是查询语言；MySQL 是 DBMS |
| **Web 框架** | Spring/Express | 框架跑在应用进程里 |
| **语言运行时** | Node/JVM | 运行时执行你的业务代码 |
| **单纯「一个文件格式」** | 有的嵌入式接近 | SQLite 是库+文件，但仍是 DBMS |

## 下一步

**数据库服务** — 为何多是独立进程；  
**中间件视角** — 为何工程上叫中间件。  
`;
