/** 工具调用（行动段） */
export default `# 工具调用

> **本课位置**：学习路径**行动**段。  
> **先修**：会话补全接口；**检索增强生成**（知识可外挂，与「点菜」正交）。  
> **下一课**：**模型上下文协议**（标准插座；紧跟本课，再谈智能体）。

## 定义

**工具调用（tool calling）**，早期也叫 **函数调用（function calling）**：  
模型不直接碰你的数据库或磁盘，而是输出结构化意图——「要调哪个工具、参数是什么」；由**你的运行时**执行，再把结果以工具角色消息回灌，模型再继续说或继续调。

| 名词 | 含义 |
|------|------|
| **工具定义（tools）** | 菜单：名称、说明、参数的 JSON Schema |
| **工具调用（tool_calls）** | 模型点的菜 |
| **工具结果消息** | 后厨上菜：\`role\` 为 tool（或等价）的回传 |

\`\`\`mermaid
sequenceDiagram
  participant M as 大语言模型
  participant App as 本仓运行时
  participant F as 真实工具
  App->>M: 消息列表 + 工具定义
  M-->>App: 工具调用
  App->>F: 执行
  F-->>App: 结果
  App->>M: 工具结果消息
  M-->>App: 自然语言或下一轮调用
\`\`\`

---

## 为什么需要它

| 痛点 | 说明 |
|------|------|
| 胡编外部事实 | 模型不知道此刻库存、天气、你的私有库 |
| 「请输出 JSON」太脆 | 自由文本再正则解析失败率高 |
| 要接真实系统 | 改文件、发消息、查库必须走你控制的代码 |

---

## 和「模型上下文协议」的差别（先记边界）

| | 工具调用 | 模型上下文协议（后文专课） |
|--|----------|----------------------------|
| 像什么 | **点菜的语法**（这一次请求怎么说） | **厨房电器的统一插座**（工具如何被发现与复用） |
| 本课范围 | 模型侧如何提出调用 | 标准插接与多客户端 |

---

## 本仓怎么做

| 行为 | 落点 |
|------|------|
| 统一走大语言模型的工具调用 | \`AiWorkflow.callAI\` → 工厂客户端；**禁止**文本假 ReAct |
| 注册可调工具 | 各工作流 \`registerMCPTool\` |
| 多工作流工具并集 | \`mergeWorkflows\`；网页/浏览器等框架工具面自动并入 |
| 请求侧白名单 | HTTP v3 的工作流名列表（streams） |
| 工具轨迹进下一轮上下文 | \`recordToolCallResult\` 写入会话笔录（见对话管线） |
| 工具轮用尽再收口 | 各 LLM 客户端可再发一轮无工具 **finalize**（\`tool-loop-finalize\`） |
| 执行前统一门禁 | \`MCPServer.handleToolCall\`：策略 / 威胁扫描 / 审批（见 **提示安全**） |
| 参数解析一次 | \`parse-tool-arguments.js\`（适配器只预览，勿双解析） |

\`\`\`quiz
{"title":"工具调用","questions":[{"q":"工具调用的关键是？","choices":[{"t":"模型输出结构化意图，由运行时执行并回填","ok":true,"why":"执行权在运行时。"},{"t":"模型直接改任意文件且无需协议","ok":false,"why":"必须经你允许的工具层。"},{"t":"只能调用浏览器","ok":false,"why":"工具可以是任意接口。"}]},{"q":"工具轮数用尽后本仓还可？","choices":[{"t":"静默丢弃，永远不再请求模型","ok":false,"why":"可 finalize 收口。"},{"t":"再发一轮无工具 finalize，让模型用已有结果写正文","ok":true,"why":"tool-loop-finalize。"},{"t":"自动微调基座权重","ok":false,"why":"无关。"}]}]}
\`\`\`

## 下一课

点菜语法之后——**模型上下文协议**（统一发现与复用工具的插座）。智能体循环在插座之后。
`;
