export default `# 系统的本质

> 操作系统是 **资源与抽象的立法者**：谁使用 CPU、内存、磁盘与网卡，均经其调度与鉴权。  
> 本课建立 OS 核心模型，供终端报错与网络端口问题倒推。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 抽象 | 进程、文件、套接字能各举业务为何在乎 |
| 权限 | 知道用户态/内核与权限相关 |
| 边界 | OS 管资源，不是业务框架 |
| 跟 Agent | 排障时区分「进程没起」与「业务 500」 |


## 本课分块

| 块 | 目标 |
|----|------|
| **为何需要 OS** | 多程序并存时的协调问题 |
| **四大抽象** | 进程、虚拟内存、文件、套接字；**跨系统同一** |
| **进程与线程** | 执行流的两层粒度 |
| **权限模型** | 用户、组、权限位与常见报错 |
| **内核 / 用户态** | 特权边界与系统调用 |
| **现象对照** | 后文故障如何落回本课 |
| **八股 × 业务** | 进程/线程、用户态、缺页、OOM |

---

## 1. 为何需要操作系统

\`\`\`flip
{"title":"没有 OS 会怎样","cards":[{"front":"多程序抢 CPU","back":"没有调度时，谁占用处理器、何时让出，只能靠程序自觉——一不小心就互锁或饿死。"},{"front":"直接摸硬件","back":"每个应用自己写驱动、自己管互斥，组合一多就崩；OS 把硬件收成统一规则再借出去。"},{"front":"OS 一句话","back":"将硬件收敛为规则，再把规则借给全部应用——资源与抽象的立法者。"}]}
\`\`\`

### 跨系统同一抽象（先角色）

\`\`\`compare
{"title":"OS 四大抽象 · 各系统都有","caption":"换系统只换实现细节，不换问题。","items":[{"role":"进程","win":"任务管理器","linux":"ps / top","mac":"活动监视器","note":"调度与隔离的基本单位"},{"role":"虚拟内存","win":"各进程独立地址空间","linux":"页表 / swap","mac":"同上抽象","note":"程序以为独占，内核在底下映射"},{"role":"文件路径","win":"盘符 C:\\\\…","linux":"单根 / …","mac":"单根 / …","note":"详见番外本机目录"},{"role":"套接字 / 端口","win":"资源监视器","linux":"ss / netstat","mac":"lsof -i","note":"Clash / Node listen 同属这一层"}]}
\`\`\`

无 OS 时，程序需自行驱动设备、互斥占用 CPU、防止踩踏内存。多程序并存会失控。  
OS 的首要职责：**将硬件收敛为规则，再将规则借给全部应用。**

\`\`\`mermaid
flowchart TB
  subgraph apps[用户态 · 应用]
    A[浏览器]
    B[Shell]
    C[Node 进程]
  end
  subgraph kernel[内核]
    S[调度]
    M[内存]
    F[文件]
    N[网络栈]
  end
  subgraph hw[硬件]
    CPU[CPU]
    RAM[内存]
    DISK[磁盘]
    NIC[网卡]
  end
  apps --> kernel --> hw
\`\`\`

---

## 2. 四大抽象

\`\`\`match
{"title":"四大抽象配对","pairs":[{"id":"proc","left":"进程 / 线程","right":"执行流；内核做时间片与上下文切换"},{"id":"vm","left":"虚拟内存","right":"近似独占的地址空间；页表映射物理页"},{"id":"fs","left":"文件","right":"路径树上的字节流；权限与块设备"},{"id":"sock","left":"套接字","right":"通向对端的通信端点；协议栈 + 网卡"}]}
\`\`\`

| 抽象 | 程序视角 | 内核协调内容 |
|------|----------|----------------|
| **进程 / 线程** | 一条或多条执行流 | 时间片调度、上下文切换 |
| **虚拟内存** | 近似独占的地址空间 | 页表映射至物理内存 / 交换区 |
| **文件** | 路径树上的字节流 | 文件系统、块设备、权限检查 |
| **套接字** | 通向对端的通信端点 | 协议栈、网卡驱动 |

### 不同系统：实例不同，抽象同一

Windows、Linux、macOS 都是 **Operating System** 的具体产品——**立法者换人，法条类别仍是这四类**。

| 你换系统时 | 本质没变 | 外观会变（后文展开） |
|------------|----------|----------------------|
| 打开文件、\`cd\` 进目录 | 仍是「文件 / 目录树」抽象 | 路径写法：\`C:\\Users\` vs \`/home\` |
| 跑 \`node\` / \`git\` | 仍是「进程 + 可执行文件」 | 可执行文件落在哪、PATH 怎么配 |
| 权限报错 | 仍是「身份 + 鉴权」 | 权限位 vs ACL 的细貌 |
| 环境变量 | 仍是进程启动时的键值配置 | 持久化写进点文件还是系统设置 |

关联学习：**先抓住抽象，再记某系统的路径与命令。**  
目录角色对照 → 番外 **本机目录**；发令窗口 → 第一章 **不同终端环境**。

<details>
<summary>打开文件的大致路径</summary>

1. 应用发起 \`open\` / \`fopen\`（最终进入系统调用）  
2. 陷入内核，校验身份与路径权限  
3. 返回 **文件描述符（fd）**  
4. 后续 \`read\` / \`write\` 均携带该 fd 由内核代劳  

</details>

---

## 3. 进程与线程

\`\`\`quiz
{"title":"进程 vs 线程","questions":[{"q":"端口被占、内存暴涨、进程崩溃隔离——排障时第一问通常是？","choices":[{"t":"先改 DNS","ok":false,"why":"那是网络命名层，不是本课排障口诀。"},{"t":"是哪个进程","ok":true,"why":"资源与隔离边界在进程；再视情况看线程是否空转。"},{"t":"先重装显卡驱动","ok":false,"why":"过度反应。"}]}]}
\`\`\`

| 概念 | 含义 | 工程直觉 |
|------|------|----------|
| **进程** | 资源分配的基本单位（独立地址空间） | \`node app\` 通常对应一个主进程 |
| **线程** | 进程内的执行流，共享多数资源 | 同一进程内可并发处理多请求 |
| **并发** | 宏观上「同时推进」 | 单核也可靠时间片交错 |

排障口诀：端口占用、内存爆炸、崩溃隔离——先问 **是哪个进程**；CPU 打满但内存不大——再看 **线程是否空转**。

---

## 4. 权限模型

\`\`\`reveal
{"title":"Permission denied 先想什么","prompt":"先读报错，再点开","tone":"warn","face":"Permission denied / Operation not permitted","body":"当前身份无权读/写/执行该路径，或内核策略（沙箱、保护）拒绝。回溯：用户/组是谁 + 权限位/ACL + 路径是否真存在。第一章 PATH 类问题也常落回「身份 + 权限 + 路径」。"}
\`\`\`

| 概念 | 说明 |
|------|------|
| **用户 / 组** | 身份与批量授权单位 |
| **权限位** | 读 / 写 / 执行（文件与目录含义不同） |
| **提权** | \`sudo\` 等临时获得更高权限（慎用） |

| 现象 | 常见含义 |
|------|----------|
| Permission denied | 当前身份无权读/写/执行该路径 |
| Operation not permitted | 内核策略拒绝（权限、沙箱、保护） |

第一章终端与 PATH 问题，多数可回溯到「身份 + 权限位 + 路径是否存在」。

---

## 5. 内核与用户态

\`\`\`flip
{"title":"特权边界翻卡","cards":[{"front":"用户态","back":"普通应用所在；不能直接摸硬件与关键表。"},{"front":"内核","back":"特权环境；调度、内存、文件、网络栈在此协调。"},{"front":"系统调用","back":"用户态请求内核代劳的正规入口（read/write/socket/fork…）。"}]}
\`\`\`

- **内核**：可直接操作硬件与关键表的特权环境  
- **用户态**：普通应用；通过 **系统调用** 请求内核代劳  

\`\`\`mermaid
sequenceDiagram
  participant U as 用户
  participant Sh as Shell
  participant OS as 内核
  participant HW as 硬件
  U->>Sh: 输入命令
  Sh->>OS: fork / exec 等
  OS->>HW: 调度与设备访问
  HW-->>OS: 完成
  OS-->>Sh: 退出码 / 输出
\`\`\`

---

## 6. 现象对照

\`\`\`match
{"title":"现象 → 底层","pairs":[{"id":"perm","left":"Permission denied","right":"权限或身份不足"},{"id":"addr","left":"Address already in use","right":"端口已被其他进程占用"},{"id":"space","left":"No space left","right":"文件系统或配额耗尽"},{"id":"oom","left":"OOM / 被杀进程","right":"内存压力与调度策略"}]}
\`\`\`

| 现象 | 底层含义 |
|------|----------|
| Permission denied | 权限或身份不足 |
| Address already in use | 端口已被其他进程占用 |
| No space left | 文件系统或配额耗尽 |
| OOM / 被杀进程 | 内存压力与调度策略 |

---

## 7. 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **进程 vs 线程（Process vs Thread）** | 进程是资源分配单位（内存、文件等），线程是进程里的一条执行流。 | Node 主进程 + worker、Nginx 多 worker 进程隔离崩溃；同一进程内多线程共享变量要加锁。 | 线程 ≠ 独立进程；杀线程不会自动释放进程占的全部资源。 |
| **用户态 / 内核态（User Mode / Kernel Mode）** | 普通应用程序跑在用户态，只有内核才能直接操作硬件和关键资源。 | 每次读文件、发网络包都要「陷入内核」，频繁切换会拖慢高 QPS 服务。 | 用户态权限低 ≠ 程序跑在虚拟机里；虚拟机是更外层的隔离。 |
| **系统调用（System Call，syscall）** | 用户态程序请求内核代为完成特权操作的唯一正规入口。 | \`read\`/\`write\`/\`socket\`/\`fork\` 最终都是 syscall；用 \`strace\` 可看见进程在调什么。 | 系统调用 ≠ 语言标准库函数；库函数可能封装多次 syscall。 |
| **上下文切换（Context Switch）** | CPU 暂停当前任务，保存现场，换去执行另一个任务。 | 线程开太多、goroutine 暴涨时，CPU 大量时间花在切换而非干活，吞吐反而掉。 | 上下文切换 ≠ 协程 yield；后者可能只在用户态调度，不一定进内核。 |
| **虚拟内存 / 缺页（Virtual Memory / Page Fault）** | 每个进程看到连续虚拟地址，OS 按需把页映射到物理内存；不在内存则触发缺页装入。 | Java/Node 堆看起来很大，真用满会狂换页，磁盘灯狂闪、接口延迟尖刺。 | 缺页 ≠ 程序一定 OOM；首次访问合法内存也会缺页，属正常。 |
| **OOM Killer（Out-Of-Memory Killer，内存耗尽杀进程）** | 物理内存不够时，Linux 内核按策略杀掉部分进程以保系统存活。 | K8s 容器没设 \`memory limit\` 时，宿主机 OOM 可能随机干掉你的 Pod；日志里常见 \`Killed\`。 | OOM Killer ≠ 应用自己 catch 到的 \`ENOMEM\`；后者是 syscall 返回错误。 |
| **文件描述符 fd（File Descriptor）** | 内核给进程打开的每个文件、套接字、管道分配的小整数编号。 | 「Too many open files」= fd 打满；长连接、连接池泄漏、忘记 \`close\` 都会踩坑。 | fd 是进程视角的编号；同一文件被两个进程打开会有两个不同 fd。 |
| **权限位 / umask（Permission Bits / umask）** | 文件谁能读、写、执行由权限位决定；umask 决定新建文件默认去掉哪些权限。 | CI 部署写 \`/var/log\` 报 \`Permission denied\`、脚本不可执行，先 \`ls -l\` 看权限与属主。 | 权限位管「谁能访问文件」；ACL 是更细粒度的扩展，不是同一套语法。 |

考证/面试常把 OS 与网络串起来：套接字在 Linux 里也是一种 fd；\`Address already in use\` 是端口被占用的进程问题，不是 DNS 问题。

## 下一步

同章：**软硬件联动**、**处理单元与存储**；  
出章：**不同终端环境**（向 OS 发令）；  
跨系统目录角色：番外 **本机目录**（先角色后路径）。
## 导图2 · 终端 / 环境变量 / 后端 × OS 本质

> 导图2 终端与后端假设有进程模型；本课钉 OS 抽象。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **终端命令行** | 对进程发令 | 壳启动的是进程 |
| **环境变量** | 进程环境 | 子进程常继承 |
| **后端** | 服务即进程 | 端口/fd 都在 OS 抽象上 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
