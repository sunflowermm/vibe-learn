/** 混合检索：关键词 + 向量 */
export default `# 混合检索

> **本课位置**：分块与嵌入之后的**召回层**。  
> **先修**：Embedding 直觉、分块策略。  
> **要点**：向量擅同义；关键词 / BM25 擅专名与编号——多数生产 RAG 两者都要。  
> **对照**：像查字典既要「近义词」（语义）也要「精确词条」（字面）——单押一侧都会漏。

## 定义

**混合检索（hybrid search）**：同一查询并行（或级联）跑**稀疏检索**（如 BM25）与**稠密检索**（向量近邻），再融合候选集。

| 通道 | 强项 | 弱项 |
|------|------|------|
| BM25 / 关键词 | 精确词、错误码、人名编号 | 同义改写弱 |
| 向量 | 语义相近、换说法 | 专名易漂 |
| 融合 | 覆盖互补 | 要调权重 / 算法 |

常见融合：**倒数排名融合（RRF）**、加权分数；之后常接 **重排**。

\`\`\`mermaid
flowchart TB
  Q[用户问题] --> BM25[BM25 / 关键词]
  Q --> VEC[向量近邻]
  BM25 --> FUSE[融合候选]
  VEC --> FUSE
  FUSE --> RR[重排可选]
  RR --> CTX[进提示]
\`\`\`

## 何时几乎必须混合

| 场景 | 原因 |
|------|------|
| 工单 / 报错码 | 字面强信号 |
| 法规条款号 | 编号不能语义漂 |
| 代码符号 | 分词与大小写敏感 |
| 用户乱换说法问同一事 | 向量补召回 |

\`\`\`flip
{"title":"混合翻卡","cards":[{"front":"只上向量？","back":"专名/代码符号常翻车"},{"front":"只上关键词？","back":"用户换说法就漏"},{"front":"RRF 是什么","back":"按各通道排名倒数相加，少调参"},{"front":"融合后还要重排吗","back":"常要：宽召回求全，重排求准"}]}
\`\`\`

\`\`\`quiz
{"title":"混合检索","questions":[{"q":"用户问「报错 ECONNRESET 怎么办」，更该依赖？","choices":[{"t":"只靠语义向量，忽略字面","ok":false,"why":"错误码是字面强信号。"},{"t":"混合：关键词保住编号，向量补解释性文档","ok":true,"why":"专名 + 语义互补。"},{"t":"立刻微调基座模型","ok":false,"why":"检索层问题先别上微调。"}]}]}
\`\`\`

## 下一步

**重排序**把融合后的候选压成真正进提示的几段。
`;
