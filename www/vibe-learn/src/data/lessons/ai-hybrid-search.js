/** 混合检索：关键词 + 向量 */
export default `# 混合检索

> **本课位置**：分块与嵌入之后的**召回层**。  
> **先修**：Embedding 直觉、分块策略。  
> **要点**：向量擅同义；关键词 / BM25 擅专名与编号——多数生产 RAG 两者都要。  
> **对照**：像查字典既要「近义词」（语义）也要「精确词条」（字面）——单押一侧都会漏。

## 定义

**混合检索（hybrid search）**：同一查询并行（或级联）跑**稀疏检索**（如 BM25）与**稠密检索**（向量近邻），再融合候选集。

| 通道 | 强项 | 弱项 |
|------|------|------|
| BM25 / 关键词 | 精确词、错误码、人名编号 | 同义改写弱 |
| 向量 | 语义相近、换说法 | 专名易漂 |
| 融合 | 覆盖互补 | 要调权重 / 算法 |

常见融合：**倒数排名融合（RRF）**、加权分数；之后常接 **重排**。

\`\`\`mermaid
flowchart TB
  Q[用户问题] --> BM25[BM25 / 关键词]
  Q --> VEC[向量近邻]
  BM25 --> FUSE[融合候选]
  VEC --> FUSE
  FUSE --> RR[重排可选]
  RR --> CTX[进提示]
\`\`\`

## 何时几乎必须混合

| 场景 | 原因 |
|------|------|
| 工单 / 报错码 | 字面强信号 |
| 法规条款号 | 编号不能语义漂 |
| 代码符号 | 分词与大小写敏感 |
| 用户乱换说法问同一事 | 向量补召回 |

\`\`\`flip
{"title":"混合翻卡","cards":[{"front":"只上向量？","back":"专名/代码符号常翻车"},{"front":"只上关键词？","back":"用户换说法就漏"},{"front":"RRF 是什么","back":"按各通道排名倒数相加，少调参"},{"front":"融合后还要重排吗","back":"常要：宽召回求全，重排求准"}]}
\`\`\`

## 本仓怎么做

| 概念 | 落点 |
|------|------|
| 小知识库 | \`tools.read\` / 工作区全文：相当于「精确读文件」，常比硬上向量库更稳 |
| 大语料 | 记忆/知识库工作流若提供检索，优先确认是否同时支持字面与语义 |
| 专名问题 | 办事助手查报错码、配置键名时，字面命中优先于「语义像」 |
| 真源 | \`docs/agent-context.md\` · \`docs/ai-workflow.md\` |

\`\`\`quiz
{"title":"混合检索","questions":[{"q":"用户问「报错 ECONNRESET 怎么办」，更该依赖？","choices":[{"t":"只靠语义向量，忽略字面","ok":false,"why":"错误码是字面强信号；纯语义常漂到无关「连接失败」散文。"},{"t":"混合：关键词保住编号，向量补解释性文档","ok":true,"why":"专名用稀疏通道锁住，语义通道再补同义/说明文档。"},{"t":"立刻微调基座模型","ok":false,"why":"这是召回层问题；先调检索再谈改权重。"},{"t":"把报错码从问题里删掉再检索，避免干扰嵌入","ok":false,"why":"删掉最强字面信号只会更难命中正确文档。"}]},{"q":"RRF（倒数排名融合）相对「直接加原始分数」的实务优点？","choices":[{"t":"按名次融合，少做跨量纲校准","ok":true,"why":"BM25 与向量分数量纲不同；RRF 用排名更稳。"},{"t":"自动加密租户数据","ok":false,"why":"RRF 不是安全机制。"},{"t":"等于交叉编码器重排","ok":false,"why":"融合在召回侧；交叉编码是精排。"},{"t":"用了就不必评测","ok":false,"why":"K 与通道仍要回归。"}]}]}
\`\`\`

## 下一步

**重排序**把融合后的候选压成真正进提示的几段。
`;
