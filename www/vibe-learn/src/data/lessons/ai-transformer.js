/** 变换器 Transformer（计算段 · 接在注意力之后） */
export default `# 变换器

> **本课位置**：学习路径**计算**段。  
> **先修**：**注意力**（自注意力、查询/键/值、因果掩码）。  
> **下一课**：**不止变换器**（选读），再进入 **自适应总览**。

## 定义

**变换器（Transformer）** = 以 **自注意力 + 前馈网络（Feed-Forward Network）** 为基本块、可堆叠多层的序列模型架构（论文 *Attention Is All You Need*，2017）。  

| 变体直觉 | 说明 |
|----------|------|
| **仅解码器**（decoder-only） | 带**因果掩码**的自回归生成；多数对话大语言模型属于此类 |
| **编码器-解码器** | 一侧编码、一侧生成；经典机器翻译等 |

\`\`\`steps
{"title":"一层变换器在干什么","steps":[{"title":"输入","body":"令牌嵌入 + 位置信息（告诉模型顺序）。"},{"title":"自注意力","body":"窗口内令牌按相关性混合（上一课）。"},{"title":"前馈网络","body":"对每个位置再做非线性变换。"},{"title":"残差与归一化","body":"稳定训练，才能堆很多层。"}]}
\`\`\`

---

## 相对更早的循环网络，它缓解了什么

| 旧约束（循环网络时代常见） | 变换器侧的答案 |
|----------------------------|----------------|
| 难并行：一步依赖上一步 | 注意力可用矩阵运算，更好吃满算力 |
| 隔很远的词信号容易淡 | 窗口内任意位置可直接交互（仍受窗口与掩码限制） |
| 难堆很深 | 残差结构支撑很深的网络 → 「大模型」层数故事 |

\`\`\`mermaid
flowchart TB
  In[嵌入 + 位置] --> Block[自注意力 + 前馈 × 多层]
  Block --> Out[下一个令牌的概率分布等]
\`\`\`

---

## 工程上会听见的词（会认即可）

| 说法 | 含义 |
|------|------|
| 层数 / 隐层宽度 | 容量与算力成本 |
| 仅解码器 | 生成时不能偷看未来令牌 |
| 混合专家（Mixture of Experts） | 多个「专家」子网络，每次只激活一部分（下一课展开） |

---

## 本仓怎么做

调用的 GPT / Claude / Llama 等，口语常是「变换器语言模型」。  
本仓改的是 **窗口里的内容与可调用工具**（对话管线、办事助手），**不是**改注意力公式或层数。

| 你改的 | 你不改的 |
|--------|----------|
| 系统说明、工作区、规则、技能目录、历史笔录 | 模型内部变换器权重结构 |
| \`mergeWorkflows\` 工具白名单 | 厂商训练用的层配置 |

\`\`\`quiz
{"title":"变换器","questions":[{"q":"多数对话大语言模型生成时常用哪种掩码？","choices":[{"t":"无掩码，任意看未来令牌","ok":false,"why":"那样等于偷看还没写的答案。"},{"t":"因果掩码：只能看过去与自身","ok":true,"why":"自回归生成的条件。"},{"t":"只看系统说明、忽略用户","ok":false,"why":"窗口内角色消息都参与。"}]}]}
\`\`\`

## 下一课

先打破「只有变换器一种身子骨」——**不止变换器**；然后 **自适应**：同一骨干如何适配任务。
`;
