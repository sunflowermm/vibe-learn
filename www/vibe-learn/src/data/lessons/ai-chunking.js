/** 分块策略：RAG 入库前的切段 */
export default `# 分块策略

> **本课位置**：检索增强生成之后的**工程细化**。面经与开源题库里大量考「chunk 怎么切」。  
> **先修**：Embedding 直觉、检索增强生成。  
> **要点**：切段决定检索粒度；太碎丢语境，太大噪声多、易撑爆窗口。

## 定义

**分块（chunking）**：把长文档切成可嵌入、可检索的片段，再写入向量库/关键词索引。

| 策略 | 白话 | 风险 |
|------|------|------|
| 固定长度 | 按字符/令牌硬切 | 切断句子与表格 |
| 重叠窗 | 相邻块留重叠 | 冗余升、成本升 |
| 结构感知 | 按标题/段落/代码块 | 依赖解析质量 |
| 语义切 | 按主题边界 | 实现更重 |

\`\`\`mermaid
flowchart LR
  DOC[长文档] --> PARSE[解析/清洗]
  PARSE --> CHUNK[分块]
  CHUNK --> EMB[嵌入]
  EMB --> IDX[索引]
\`\`\`

## 实务口诀

| 场景 | 倾向 |
|------|------|
| 政策/制度条文 | 结构感知，保留条款号 |
| 代码与 API | 按符号/文件，忌切断签名 |
| FAQ 短答 | 一块一问一答 |
| PDF 多栏 | 先版面还原，再切；必要时 OCR |

## 和窗口预算的关系

分块不是越细越好：

| 过碎 | 过大 |
|------|------|
| 缺主语/条款语境 | 噪声多、超窗口、嵌入被稀释 |
| 重排候选爆炸 | 一次检索带回无关段落 |

经验：先按**结构**切，再对难例做**上下文前缀**（给 chunk 标明出自哪章/哪家公司），用评测集验证，而不是凭感觉改长度。

\`\`\`quiz
{"title":"分块","questions":[{"q":"分块过碎最常见的后果？","choices":[{"t":"检索永远召不回","ok":false,"why":"仍可能召回，但语境残缺。"},{"t":"片段缺上下文，生成易断章取义","ok":true,"why":"粒度决定可读证据质量。"},{"t":"嵌入维度会自动变大","ok":false,"why":"维度由嵌入模型决定。"}]},{"q":"改 chunk 大小后，工程上最少该做什么？","choices":[{"t":"用固定问题集看召回是否变好/变差","ok":true,"why":"没有评测就是玄学调参。"},{"t":"立刻全量微调基座","ok":false,"why":"先验证检索。"},{"t":"删掉 HTTPS","ok":false,"why":"无关。"}]}]}
\`\`\`

## 下一步

**混合检索**（关键词补专名）· **重排序**（精排 Top-K）。
`;
