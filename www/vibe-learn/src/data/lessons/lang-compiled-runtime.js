export default `# 编译型与运行时型

> 源码是给人读的；机器要的是可调度的指令。  
> 差异在于：**翻译发生在何时、由谁完成**。  
> **学会之后**：能用「发布时间付清 vs 边跑边付」解释 Go/Rust 与 Node/Python 的交付差异，并说明本仓为何盯 Node 版本。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 两条路径 | 能口述 AOT/编译型与运行时型差异各一句 |
| 光谱 | 不说非黑即白；知道 JIT/字节码常见 |
| 本仓 | 说明为何盯 Node 版本（运行时契约） |
| 跟 Agent | 问分发/启动问题时标明目标平台与运行时 |

## 本课分块

| 块 | 内容 |
|----|------|
| **两条经典路径** | 先编译 vs 靠运行时 |
| **光谱而非二分** | AOT / JIT / 字节码 |
| **对照表** | 分发、启动、改完再跑 |
| **接到 XRK** | 为何本仓盯 Node 版本 |

| 词 | 白话 |
|----|------|
| **编译型（AOT）** | 多在发布前译成机器码/可执行文件；目标平台要匹配 |
| **运行时型** | 边跑边译（或经 VM）；常要预装运行时 |
| **光谱** | AOT + JIT、字节码中间层很常见，不是非黑即白 |

---

## 1. 两条经典路径

\`\`\`mermaid
flowchart TB
  subgraph aot["编译型 · ahead-of-time"]
    S1[源码] --> C[编译器]
    C --> B["机器码 / 可执行文件"]
    B --> P1["OS 加载为进程"]
  end
  subgraph rt["运行时型"]
    S2[源码] --> R["运行时 / VM"]
    R --> P2[进程内执行]
  end
\`\`\`

### 编译型（ahead-of-time）

- 代表直觉：C、C++、Go、Rust（细节各异）  
- 特点：发布物常是二进制；**目标平台要匹配**（或交叉编译）  
- 改完再跑：通常要重新编译（或增量编译）

### 解释 / 运行时型（with runtime）

- 代表直觉：Python、Ruby；JavaScript 在引擎中的执行  
- 常配合 **虚拟机 / 字节码**（Java、C#）：先编译到中间码，再由 VM 执行  
- 机器上通常要先装好对应运行时（\`node\`、\`python\`、\`java\`…）

现实是光谱：许多语言 **AOT + JIT** 并存；「纯解释」今天很少见。

| 词 | 白话 |
|----|------|
| **AOT** | Ahead-Of-Time：发布前译完 |
| **JIT** | Just-In-Time：跑起来后对热点再优化 |
| **字节码** | 面向 VM 的中间码（如 JVM \`.class\`），不是最终机器码 |

---

## 2. 对照表

| | 编译到本地 | 依赖运行时 |
|--|------------|------------|
| 分发 | 常较大、贴平台 | 源码/字节码 + 需预装运行时 |
| 启动 | 可很快 | 先起 VM/引擎 |
| 改完再跑 | 通常要重新编译 | 改完即可再执行（或热重载） |
| 版本契约 | 二进制 ABI / 目标三元组 | **运行时主版本**（如 Node 26）常写进 engines |

\`\`\`match
{"title":"分发与启动直觉","pairs":[{"id":"dist","left":"贴平台二进制","right":"常见于编译型分发"},{"id":"rt","left":"源码 + 预装运行时","right":"常见于运行时型分发"},{"id":"jit","left":"边跑边优化","right":"JIT 一类，光谱中间态"}]}
\`\`\`

---

## 3. TypeScript 放哪？

**TypeScript** 是 JavaScript 的**超集**（多了类型等）；交付到 Node/浏览器前通常要**擦除类型 → 仍是 JS**。  
它不另起一个「TS 运行时」当主路径：本仓主服仍按 **Node 执行 JS** 理解。细节见 **TypeScript** 分课与 01.5「TS 动手」。

---

## 4. 和 XRK 的关系

| 点 | 含义 |
|----|------|
| 主服模型 | **JavaScript + Node.js 运行时** → 必须装对齐的 Node，不是「编译出一个免安装 exe」当默认路径 |
| 为何卡 engines | 运行时型交付的版本契约；\`node -v\` 过低会直接不符合仓库假设 |
| 子服 | Go/Rust 等更偏编译交付；Python/Java 等自有运行时——边界在 **接到本仓** / 第四章语言栈 |
| 浏览器 www | 另一套运行时（浏览器引擎）；与主服 Node **不要混 API** |

\`\`\`quiz
{"title":"编译与运行时","questions":[{"q":"本仓主服最贴近哪一类语言/运行时交付形态？","choices":[{"t":"纯 AOT：发布一个与 Node 无关的 exe","ok":false,"why":"主服默认依赖 Node 进程。"},{"t":"源码/构建产物 + 预装 Node 运行时","ok":true,"why":"pnpm install 后 node 执行。"},{"t":"只能在浏览器里跑","ok":false,"why":"主服是 Node；www 才是浏览器。"}]},{"q":"Go 子服与 Node 主服差异，更贴切的是？","choices":[{"t":"Go 一定更「高级」","ok":false,"why":"不是高级低级，是模型不同。"},{"t":"Go 常编译为二进制；Node 依赖已安装的运行时执行 JS","ok":true,"why":"翻译账单支付时机不同。"},{"t":"二者语法相同","ok":false,"why":"语言不同。"}]}]}
\`\`\`

## 下一步

**语言版图** — 把更多语言放进层次，而不是背排行榜。  
然后 **Node.js（运行时）** 钉分类；**接到本仓** 对齐契约。
`;
