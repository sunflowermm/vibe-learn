/** 数据库作为服务 */
export default `# 数据库服务

> 绝大多数生产环境里的 DBMS，以 **长时间运行的服务进程**（或云托管等价物）存在：  
> 监听端口 → 接受客户端连接 → 执行查询/命令 → 读写存储。  
> **SQLite** 是重要例外（嵌入进程、文件库），见专课。

## 本课分块

| 块 | 目标 |
|----|------|
| **客户端–服务器** | 谁连谁 |
| **进程与端口** | 本机 / 远程 |
| **生命周期** | 安装、启动、监控 |
| **托管形态** | 自建 vs 云数据库 |

---

## 1. 客户端–服务器

\`\`\`mermaid
flowchart LR
  App[应用进程<br/>Node / Java / …] -->|驱动协议| Svc[数据库服务进程]
  Svc --> Store[(内存 / 磁盘)]
\`\`\`

| 角色 | 做什么 |
|------|--------|
| **服务端（DBMS 进程）** | 管数据、权限、持久化、并发 |
| **客户端** | 你的应用里的驱动（\`redis\` npm、\`pg\`、Mongo driver…）发协议命令 |
| **管理端** | \`redis-cli\`、\`psql\`、GUI 工具——也是客户端 |

应用 **不会** 把整份 PostgreSQL 源码链进自己；它只是 **连上服务**。

---

## 2. 进程、端口、本机与远程

| 概念 | 说明 |
|------|------|
| **监听端口** | 如 Redis \`6379\`、PostgreSQL \`5432\`、MySQL \`3306\`、MongoDB \`27017\`（可改） |
| **本机开发** | \`127.0.0.1:端口\`；防火墙与绑定地址要留意 |
| **远程 / 容器** | Docker Compose 服务名、云内网地址；应用配连接串 |
| **鉴权** | 密码、ACL、TLS——生产必开 |

本仓 Redis：启动链 \`ensure-redis\` 探测本机服务（**装机**见部署环境；**契约**见第四章数据与缓存）。

---

## 3. 运维生命周期（基础）

1. **安装** DBMS 或拉镜像  
2. **配置** 数据目录、内存、密码、绑定地址  
3. **启动** 为服务 / 容器 / 前台进程  
4. **健康检查** ping、慢查询、磁盘、复制延迟  
5. **备份与升级** 按产品文档  

开发机最小路径：能 \`ping\` / 连上 → 再跑应用。

---

## 4. 自建 vs 托管

| 形态 | 你负责什么 | 直觉 |
|------|------------|------|
| **自建进程** | 安装、PATH、开机、备份 | Windows Redis zip 即此类 |
| **Docker Compose** | 容器编排与卷 | 本仓 \`docs/docker.md\` 含 redis |
| **云数据库 / DBaaS** | 连接串与权限；少管机器 | RDS、Atlas、云 Redis 等 |

## 下一步

**中间件视角** — 服务夹在业务之间；  
**版图与流行度** — 常见产品地图。  
`;
