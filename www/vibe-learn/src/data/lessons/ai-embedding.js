export default `# Embedding 与向量直觉

> **本课位置**：学习路径**知识**段入口（应用直觉，不上公式专课）。  
> **先修**：令牌与窗口（材料进窗有预算）。  
> **文献 / 产品锚点**：稠密向量检索直觉；OpenAI [Embeddings](https://platform.openai.com/docs/guides/embeddings) 等产品文档；经典 RAG 见 [Lewis et al., 2020](https://arxiv.org/abs/2005.11401)。  
> **下一课**：**检索增强生成**；工程细化见分块 / 向量库 / 混合检索。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 直觉 | 能说明向量近似「语义坐标」，相近可检索 |
| 边界 | 嵌入≠生成回答；近 ≠ 事实正确；不上公式专课 |
| 衔接 | 服务 RAG / 向量库；建库与查询同模型 |
| 本仓 | 按已加载能力；概念先通 |

\`\`\`algo
{"kind":"embnear","title":"问句向量 · Top-K 近邻","autoplay":true,"speed":780}
\`\`\`

\`\`\`check
{"title":"Embedding 通关","items":[{"id":"space","text":"能说明同模型同维度是近邻有意义的前提","hint":"对齐"},{"id":"notgen","text":"知道检索近邻 ≠ 已生成正确答案","hint":"边界"},{"id":"bm25","text":"能说出相对关键词：擅同义、弱专名","hint":"对照"}]}
\`\`\`

## 心智图

**Embedding（嵌入）**：把文本（或图/音）变成一串固定长度的数字——**向量**。意思接近的文本，在空间里往往也靠近。  
**经典 RAG** 常用：问句 → 向量 → 近邻文档块 → 塞进提示词 → LLM 生成。

\`\`\`mermaid
flowchart LR
  T[文本块] --> E[Embedding 模型]
  E --> V[向量]
  V --> IDX[向量库 / 索引]
  Q[用户问题] --> E2[同一套嵌入模型]
  E2 --> VQ[问句向量]
  VQ --> IDX
  IDX --> CHUNKS[Top-K 相近片段]
  CHUNKS --> LLM[拼进上下文生成]
\`\`\`

| 词 | 白话 |
|----|------|
| **维度** | 向量有多长（如 384 / 768 / 1536…）；同库须同模型同维度 |
| **相似度** | 常用**余弦相似度**或内积；近 ≠ 事实正确 |
| **分块 chunk** | 长文切段再嵌入；太碎丢上下文，太大噪声多、超窗口 |
| **Top-K** | 取最相近的 K 段；K 大噪音增，K 小易漏 |
| **同一套模型** | 建库与查询必须用**同一个** embedding 模型，否则空间不对齐 |

---

## 和关键词检索差在哪

| | 关键词 / BM25 | 向量检索 |
|--|---------------|----------|
| 匹配 | 字面、词项统计 | 语义邻近 |
| 同义改写 | 弱 | 相对强 |
| 专有名词 / 编号 | 往往更稳 | 可能「语义漂」丢精确项 |
| 工程 | 倒排索引成熟 | 需选模型、维度、索引（HNSW 等） |

实务常走：**分块** → **混合检索**（关键词 + 向量）→ **重排** → 进窗生成。

## 本仓怎么做

| 概念 | 落点 |
|------|------|
| 外挂证据进窗 | 记忆 / 知识库类工作流（按已加载能力）；或 \`tools.read\` 直接读小语料 |
| 预算 | \`agentWorkspace\` 与 \`assembleChatLlmMessages\` 三层抢同一窗口 |
| 真源 | \`docs/agent-context.md\`；全章总表见章框 **本仓总对照** |

\`\`\`quiz
{"title":"Embedding","questions":[{"q":"建库用模型 A、查询用模型 B，会发生？","choices":[{"t":"一般更好，因为多样性","ok":false,"why":"不同嵌入模型学到的空间坐标不可比，近邻排序会乱。"},{"t":"近邻结果不可靠，通常要统一模型","ok":true,"why":"建库与查询必须落在同一嵌入空间，同模型同维度是前提。"},{"t":"只要维度数字相同就完全等价","ok":false,"why":"维度相同只说明向量长度一样，坐标含义仍由具体模型决定。"},{"t":"运行时会自动把 B 的向量线性变换到 A 的空间","ok":false,"why":"没有通用的「模型间自动对齐」；换模型通常要整库重嵌。"}]},{"q":"相对 BM25/关键词，向量检索更擅长、更不擅长什么？","choices":[{"t":"更擅同义改写；更不擅专名/编号等字面强信号","ok":true,"why":"语义近邻补换说法；条款号/错误码常仍要关键词通道。"},{"t":"更擅精确条款号；更不擅同义改写","ok":false,"why":"说反了：字面稳是 BM25 侧。"},{"t":"向量近邻保证事实正确，可跳过生成","ok":false,"why":"近 ≠ 正确；检索后仍要生成与忠实度。"},{"t":"换嵌入模型从不需要重嵌，索引可永久复用","ok":false,"why":"换模型通常整库重嵌。"}]}]}
\`\`\`

## 下一步

**经典 RAG** · **分块策略** · **向量库**。
`;
