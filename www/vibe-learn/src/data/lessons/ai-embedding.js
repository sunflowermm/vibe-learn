/** Embedding 与向量直觉 */
export default `# Embedding 与向量直觉

> **Embedding（嵌入）**：把文本（或图/音）变成一串固定长度的数字——**向量**。意思接近的文本，在空间里往往也靠近。  
> **经典 RAG** 常用：问句 → 向量 → 近邻文档块 → 塞进提示词 → LLM 生成。  
> 本课建立直觉；公式与训练细节不展开。

## 心智图

\`\`\`mermaid
flowchart LR
  T[文本块] --> E[Embedding 模型]
  E --> V[向量]
  V --> IDX[向量库 / 索引]
  Q[用户问题] --> E2[同一套嵌入模型]
  E2 --> VQ[问句向量]
  VQ --> IDX
  IDX --> CHUNKS[Top-K 相近片段]
  CHUNKS --> LLM[拼进上下文生成]
\`\`\`

| 词 | 白话 |
|----|------|
| **维度** | 向量有多长（如 384 / 768 / 1536…）；同库须同模型同维度 |
| **相似度** | 常用**余弦相似度**或内积；近 ≠ 事实正确 |
| **分块 chunk** | 长文切段再嵌入；太碎丢上下文，太大噪声多、超窗口 |
| **Top-K** | 取最相近的 K 段；K 大噪音增，K 小易漏 |
| **同一套模型** | 建库与查询必须用**同一个** embedding 模型，否则空间不对齐 |

\`\`\`flip
{"title":"向量翻卡","cards":[{"front":"不是关键词搜索","back":"可匹配同义改写，也可能语义漂移"},{"front":"检索 ≠ 生成","back":"RAG 还要 LLM 组织答案；检索错则胡编有据"},{"front":"换嵌入模型","back":"通常要整库重嵌入"},{"front":"本课不上公式","back":"够用即可；专课另寻"}]}
\`\`\`

## 和关键词检索差在哪

| | 关键词 / BM25 | 向量检索 |
|--|---------------|----------|
| 匹配 | 字面、词项统计 | 语义邻近 |
| 同义改写 | 弱 | 相对强 |
| 专有名词 / 编号 | 往往更稳 | 可能「语义漂」丢精确项 |
| 工程 | 倒排索引成熟 | 需选模型、维度、索引（HNSW 等） |

实务常 **混合检索**（关键词 + 向量）再重排——细节见第五章 RAG 课。

## 接到本仓 / 第五章

先看 **经典 RAG**，再回本课核对「为何要同一嵌入模型」。  
**Agentic RAG** 是多轮工具环里的检索策略，底座仍常是嵌入 +（可选）关键词。

\`\`\`quiz
{"title":"Embedding","questions":[{"q":"建库用模型 A、查询用模型 B，会发生？","choices":[{"t":"一般更好，因为多样性","ok":false,"why":"向量空间不对齐，近邻无意义。"},{"t":"近邻结果不可靠，通常要统一模型","ok":true,"why":"同一嵌入空间是前提。"},{"t":"只要维度数字相同就完全等价","ok":false,"why":"维度相同≠同一空间。"}]}]}
\`\`\`

## 下一步

**经典 RAG** · **上下文工程**。
`;
