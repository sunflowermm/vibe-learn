/** 路由、网关、NAT、防火墙 */
export default `# 路由、网关、NAT 与防火墙

> **路由**：按目标 IP 选择路径并转发数据包。  
> 家用场景常同时涉及：默认网关、NAT、以及简易防火墙规则。

## 本课分块

| 块 | 目标 |
|----|------|
| **包如何出网** | 同网段 vs 交网关 |
| **路由表直觉** | 下一跳怎么查 |
| **路由器 vs 交换机** | L3 vs L2 |
| **NAT / PAT** | 多台内网共享出口 |
| **IP 池直觉** | 动态地址从哪来 |
| **防火墙直觉** | 入站 / 出站过滤 |
| **端口转发** | 把公网端口映射到内网服务 |
| **八股 × 业务** | SNAT/DNAT、ACL、默认路由 |

---

## 1. 包怎么走

1. 源主机判断目标是否在同一网络  
2. 否 → 交给 **默认网关**  
3. 路由器查 **路由表** 决定下一跳  
4. 可能经多跳到达目标网络  

\`\`\`mermaid
flowchart LR
  H[内网主机] --> G[默认网关]
  G --> I[公网路径]
  I --> S[目标服务器]
\`\`\`

---

## 2. 路由器 vs 交换机

| | 路由器 | 交换机 |
|--|--------|--------|
| 层次 | 网络层（L3）为主 | 链路层（L2）为主 |
| 依据 | IP | MAC |
| 作用 | 连接不同网络 | 连接同一网内设备 |

---

## 3. NAT（网络地址转换）

将内网私有 IP 映射为出口公网地址（常见为 PAT：多对一靠端口区分）。

\`\`\`
内网 192.168.1.10:5000  →  公网 203.0.113.1:12345
内网 192.168.1.11:5000  →  公网 203.0.113.1:12346
\`\`\`

| 类型 | 特点 |
|------|------|
| 静态 NAT | 一对一固定映射 |
| 动态 NAT | 从地址池分配 |
| PAT / NAPT | 多对一；家庭最常见 |

### IP 池（地址池）

云与运营商场景里，常把一批公网地址放进**池**里按需分配：

| 用途 | 例子 |
|------|------|
| 动态 NAT | 出站连接临时占用池中地址 |
| 弹性 / 浮动 IP | 可在实例间迁移的「对外门牌」 |
| 负载均衡 / 代理出口 | 多入口或多出口共用一组地址 |
| 白名单业务 | 第三方 API 只放行你池中的固定出口 |

与后文 **边缘实务** 衔接：CDN/Cloudflare 对外往往是 Anycast 入口；源站仍可能绑弹性 IP，并用防火墙只允许边缘回源。

---

## 4. 防火墙直觉

**防火墙**：按规则允许或拒绝数据包通过（主机防火墙或网关防火墙）。

| 方向 | 典型问题 |
|------|----------|
| **出站** | 本机进程能否访问外网某端口 |
| **入站** | 外网能否主动连到本机监听端口 |

| 现象 | 排查方向 |
|------|----------|
| 浏览器能开网页，本地服务外网访问不到 | NAT 未做端口转发，或入站被拒 |
| \`Connection refused\` | 对端无进程监听，或被拒绝 |
| \`Timed out\` | 路径不通、丢弃或过滤 |

防火墙与 NAT 常同机配置：先有地址转换，再有「哪些端口允许进入」。

---

## 5. 端口转发（入门）

将路由器公网 IP 的某端口映射到内网主机端口，使外网可到达内网服务。

| 概念 | 说明 |
|------|------|
| 端口转发 | 网关把入站连接转到指定内网 IP:端口 |
| 与反向代理 | 转发偏「网络层/传输层入口」；反代偏「HTTP 应用层门面」 |
| 风险 | 暴露面增大；需配合防火墙与认证 |

企业里「开安全组 22/80/443」与家用端口转发是同一类问题：**允许谁从哪边进来**。

---

## 6. 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **默认路由 \`0.0.0.0/0\`（Default Route）** | 路由表里「其他所有目的地都走这一条」的兜底条目，通常指向默认网关。 | 云 VPC 里私网子网要配 \`0.0.0.0/0 → NAT 网关/IGW\` 才能上网；删了它内网机出不了公网。 | 默认路由 ≠ 默认 DNS；一个管包往哪走，一个管名字解析。 |
| **SNAT / DNAT（Source / Destination NAT，源/目的地址转换）** | SNAT 改包的源 IP（多为内网出网）；DNAT 改包的目的 IP/端口（多为公网入站进内网）。 | 家用路由出网是 SNAT；云「端口转发」「负载均衡监听」入站常用 DNAT 把公网 443 转到内网 8080。 | SNAT/DNAT 是方向概念；PAT 是 SNAT 的一种实现方式。 |
| **PAT（Port Address Translation，端口地址转换）** | 多个内网主机共享一个公网 IP，靠不同源端口区分会话，也叫 NAPT。 | 公司几百台 PC 共用一个出口公网 IP；云 NAT 网关统计「并发连接数」就是在数 PAT 表项。 | PAT ≠ 一对一静态 NAT；后者一个私网 IP 固定映射一个公网 IP。 |
| **ACL（Access Control List，访问控制列表）** | 按源/目的 IP、端口、协议等条件决定放行或丢弃的规则列表。 | 安全组「仅允许 443/TCP 入站」就是 ACL；写错协议（TCP 写成 UDP）等于规则没生效。 | ACL ≠ 应用 RBAC；ACL 管网络包，RBAC 管登录后能调哪个 API。 |
| **端口转发 vs 反向代理（Port Forward vs Reverse Proxy）** | 端口转发多在 L3/L4 把公网 IP:端口映射到内网 IP:端口；反代在 L7 解析 HTTP 再选后端。 | 开发机临时 \`ssh -R\` 或路由器 DMZ 是转发；生产 API 用 Nginx/Caddy 按 Host/Path 转发更常见。 | 端口转发通常不懂 URL；反代可以一个 443 上挂多个域名。 |
| **Hairpin NAT（发夹 NAT / 回环 NAT）** | 内网主机用公网域名访问自家服务时，包先出网关再绕回内网，部分路由器不支持会失败。 | 内网访问 \`https://自家域名\` 偶发超时，外网却正常；可改 split DNS 指内网 IP 或开 hairpin 功能。 | Hairpin 不是 DNS 污染；是 NAT 设备对「从内访问自己公网地址」的处理能力问题。 |
| **ICMP（Internet Control Message Protocol，互联网控制报文协议）** | 网络层辅助协议，用于报告错误、探测可达性，\`ping\` 常用 ICMP Echo。 | \`ping\` 通只说明网络层可达，不代表 443 端口开放；安全组禁 ICMP 会导致 ping 不通但网站仍能开。 | ICMP 不通 ≠ HTTP 不通；应用端口可能仍正常监听。 |

---

## 7. 与后文 / 番外

- **反向代理**：靠近服务器侧的应用层门面  
- **边缘实务**：IP 池产品化、回源白名单、地域出口  
- **Clash**：客户端侧代理；规则中的网段/IP 决策类似「选路」  
- **ICMP / ping**：网络层可达性探测（见协议栈补充）  

## 下一步

**DNS 与 HTTPS** · **HTTP** · **反向代理** · **边缘与出口实务**。
`;
