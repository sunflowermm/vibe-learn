export default `# 智能体图编排

> **本课位置**：学习路径**行动**段；**先修：智能体与控制循环**。  
> **典籍 / 工程锚点**：先计划后执行时常把步骤建成**有向无环图**；LangGraph 等框架用「节点 + 边」实现多种循环模式（**模式 ≠ 框架**）。  
> 相关：ReWOO 等「先规划占位、工具可并行、最后汇总」变体。  
> **下一课**：汇合段 **智能体式检索**（经典 RAG + 循环）。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 直觉 | 能说明图/DAG 表达步骤依赖与并行 |
| 边界 | 本仓主路径不是通用图编辑器 |
| 对照 | 固定消息三层+工具环 vs 任意图 |
| 跟 Agent | 不要默认引入重型编排框架 |


## 定义（先分清两种「图」）

| 说法 | 全称直觉 | 本课是否主讲 |
|------|----------|--------------|
| **智能体图 / 编排图** | 控制流：节点=步骤或智能体，边=依赖或条件转移 | **是** |
| **有向无环图**（Directed Acyclic Graph，DAG） | 有方向、无环的依赖图；可表达并行与先后 | **是** |
| **知识图谱**（knowledge graph） | 实体-关系知识表示，供检索或推理 | 否（属知识表示；勿与编排图混淆） |
| **神经网络计算图** | 训练/推理时的算子图（如变换器内部） | 否 |

**智能体图编排（agent graph）** = 用节点与边描述「谁在什么条件下做什么」，而不是只有单一线性循环。

\`\`\`mermaid
flowchart LR
  Start[开始] --> A[检索]
  Start --> B[读工作区]
  A --> C[汇总]
  B --> C
  C --> D{质量够?}
  D -->|否| A
  D -->|是| End[结束]
\`\`\`

上图：A 与 B 可并行（无互相依赖）；C 依赖二者；D 是**条件边**（conditional edge）。

---

## 为何从「一条环」走到「一张图」

| 线性循环（如单纯交替）的局限 | 图能表达的 |
|------------------------------|------------|
| 难以一眼看到全局依赖 | 计划可预览、可审查 |
| 独立子任务只能串行 | **无依赖边**的节点可并行，降时延 |
| 失败后只能「再想一步」 | 可局部重跑子图或触发重计划节点 |
| 多角色难摆 | 节点可以是不同工具集或子智能体 |

先计划后执行的常见形态：规划器输出 **DAG（节点=工具或子任务，边=依赖）** → 执行器按拓扑序（可并行）跑 → 汇总或重计划。

---

## 核心零件（全称）

| 中文 | 含义 |
|------|------|
| **节点（node）** | 一步：调工具、调模型、等人确认、子智能体… |
| **边（edge）** | 转移：固定下一节点，或按状态选下一节点 |
| **条件边** | 根据工具结果 / 检验结果走不同分支 |
| **状态（state）** | 图上流转的共享数据（消息、中间结果、步数余额） |
| **重计划节点** | 执行偏离预期时回到规划器 |
| **扇出 / 扇入** | 一到多并行；多结果汇合后再继续 |

\`\`\`match
{"title":"图编排配对","pairs":[{"id":"dag","left":"有向无环图","right":"步骤依赖且无环，可并行无依赖节点"},{"id":"cond","left":"条件边","right":"按状态选择下一节点"},{"id":"kg","left":"知识图谱","right":"实体关系知识，不是控制流"},{"id":"fw","left":"图框架","right":"实现模式的水管，不是模式本身"}]}
\`\`\`

---

## 和「智能体循环」怎么叠

| 层次 | 常见做法 |
|------|----------|
| 外层图 | 先计划后执行、分支、并行、人工审批节点 |
| 内层循环 | 某个执行节点内部仍是推理与行动交替（带工具轮预算） |
| 最外反思 | 整图跑完再做检验与修正 |

不要把「用了某图框架」等同于「一定是智能体」：很多图其实是**固定工作流**（边写死、模型只填内容）。

---

## 本仓怎么做

本仓主对话路径是 **「组装消息 + 工具调用循环」**，不是通用可随意连线的可视编排器：

| 概念 | 本仓近似落点 |
|------|----------------|
| 内层循环 | 大语言模型客户端工具轮（\`maxToolRounds\`） |
| 「有哪些节点可用」 | \`mergeWorkflows\` + 框架工具面 + 远程模型上下文协议（工具白名单） |
| 固定预处理图 | \`assembleChatLlmMessages\` 三层顺序（系统 → 易变 → 历史 → 当前）写死 |
| 并行工具 | 视提供商 \`parallel_tool_calls\` 等；业务图级并行需自建或外挂编排 |
| 子智能体图 | \`subagents.yaml\` 目前是**路由提示清单**，默认不启隔离子会话（见办事助手） |
| 真源 | \`docs/agent-context.md\` · \`docs/ai-workflow.md\` |

若要做复杂 DAG 业务（多审批、多并行流水线），通常在 Core 工作流或外部编排中实现，而不是改对话管线三层顺序。

\`\`\`quiz
{"title":"智能体图编排","questions":[{"q":"编排里的有向无环图主要表达？","choices":[{"t":"实体之间的知识关系","ok":false,"why":"那是知识图谱。"},{"t":"步骤之间的依赖与可并行关系","ok":true,"why":"控制流依赖。"},{"t":"变换器内部的注意力分数","ok":false,"why":"那是模型计算，不是智能体编排。"}]}]}
\`\`\`

## 下一课

环与图都有了——汇合段：**智能体式检索**（把检索策略交给多轮工具环）。
## 导图2 · Agent 循环 / Harness × 图编排

> 导图2 少谈图编排产品；本课对照概念与本仓主路径。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **Agent 循环** | 控制流 | 图是一种表达循环/分支的方式 |
| **Harness Engineering** | 宿主选编排 | 本仓以工作流+工具环为主 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
