/** 工作台 · 分层排障 */
export default `# 工作台 · 分层排障

> 「跑不起来」时最忌一上来改业务代码。先**分层**：环境 → 依赖 → 配置 → 代码。

## 先认词（别被缩写吓到）

| 写法 | 白话 |
|------|------|
| **PATH** | 系统找「可执行命令」时翻的目录列表；不在列表里 → \`command not found\` |
| **cwd** | current working directory = 你现在站的文件夹（\`pwd\` 打印它） |
| **依赖 / node_modules** | \`pnpm install\` 下载的第三方库目录 |
| **代理 / HTTP_PROXY** | 告诉程序「出网走哪扇门」；国内拉 GitHub 常要 |
| **堆栈** | 报错里一串 \`文件:行号\`，从下往上看调用关系 |

\`\`\`flip
{"title":"排障翻卡","cards":[{"front":"command not found","back":"多半是没装，或装了但 PATH 没生效（新开终端）"},{"front":"先改插件？","back":"错。先确认 node/pnpm/目录/依赖都绿"},{"front":"模拟窗 vs 真机","back":"下面沙箱练口诀；真失败仍要在本机终端复现"}]}
\`\`\`

## 固定口诀

\`\`\`steps
{"title":"排障分层","steps":[{"title":"命令在不在","body":"node -v / pnpm -v / git -v"},{"title":"目录对不对","body":"是否在仓库根（有 package.json）"},{"title":"依赖装没装","body":"node_modules / pnpm install"},{"title":"配置与端口","body":"yaml、代理、端口占用、Redis"},{"title":"再读报错","body":"堆栈第一帧自己的文件"}]}
\`\`\`

## 动手：模拟终端

> 假数据；练「先查工具链」。真机排障命令一样。

\`\`\`shell
{"preset":"troubleshoot"}
\`\`\`

| 层 | 检查 | 成功长什么样 |
|----|------|----------------|
| **运行时** | \`node -v\` | 类似 \`v26.x\`（本仓要 ≥ 26） |
| **包管理** | \`pnpm -v\`；根目录 \`pnpm install\` | 有版本号；install 结束无红字 |
| **网络/代理** | 装包失败？ | 回 Clash / \`HTTP_PROXY\` 课 |
| **配置** | 缺 Redis？ | 见 **部署环境** |
| **代码** | 语法/逻辑 | 上面都绿再查 |

## 和调不通模型 / Agent 的交界

同一口诀，只是多两层：

| 层 | 多查什么 |
|----|----------|
| 网络 | DNS、代理、\`HTTP_PROXY\`、TLS |
| HTTP | 401 密钥、429 配额、超时 |
| 业务 | 提示、工具、检索——**最后**才动 |

\`\`\`quiz
{"title":"排障","questions":[{"q":"pnpm: command not found 时，优先？","choices":[{"t":"先改插件业务逻辑","ok":false,"why":"命令都没有，与业务无关。"},{"t":"查 PATH / 是否安装 pnpm","ok":true,"why":"落在工具链层。"},{"t":"格式化硬盘","ok":false,"why":"过激且无关。"}]}]}
\`\`\`

## 接到本仓

首次跑通失败：按本课分层倒推，再对照 **部署环境** 清单。

## Coding Agent 协作

可复制：

\`\`\`prompt
目标：按「命令 → 目录 → 依赖 → 配置 → 代码」分层排查，我起不来主服。
现场：OS=…；当前目录=…；报错原文=…；已试过=…
约束：一次只解一层；不要一上来重写业务；需要我跑命令时给出可复制的一行。
验收：指出当前卡在哪一层；给出下一层成功信号（例如 node -v 输出形态）。
\`\`\`

## 下一步

有 Node 后进入 **编程基础**；或继续 Git / 首次跑通。
`;
