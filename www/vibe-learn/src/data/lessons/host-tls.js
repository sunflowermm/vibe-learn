/** TLS 证书实务 */
export default `# TLS 证书实务

> 浏览器地址栏的锁 = **TLS（HTTPS）**。证书证明「你连的大概是这个域名的服务端」，并加密传输。

## 三角关系

\`\`\`mermaid
flowchart LR
  D[域名解析到服务器] --> C[申请/签发证书]
  C --> N[Nginx/Caddy/面板终止 TLS]
  N --> APP[回源 HTTP 到本机端口]
\`\`\`

| 词 | 白话 |
|----|------|
| Let's Encrypt | 常见免费证书来源 |
| 续期 | 证书有过期时间；面板常自动续 |
| 终止 TLS | 在网关解密，后面可走内网 HTTP |

第三章 **DNS 与 HTTPS** 讲概念；本课钉**上机步骤直觉**。

## Coding Agent 协作

可复制：

\`\`\`
目标：域名已解析到服务器；反代已指到 127.0.0.1:主服端口。请给出申请/续期 Let's Encrypt（或面板一键证书）的步骤。
现场：面板或纯 Nginx=…；域名=…；80/443 是否已放行=…
约束：先确认 DNS 生效再申请；不要关掉仅 HTTP 的调试入口除非我确认；密钥与私钥勿贴进聊天可提交的仓。
验收：浏览器锁标出现；证书域名匹配；说明续期谁在管（面板/cron）。
\`\`\`

## 下一步

**备份** · **面板上跑 Node**。
`;
