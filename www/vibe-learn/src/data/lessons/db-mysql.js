/** MySQL */
export default `# MySQL

> **分类：开源关系型 DBMS**（生态极广：LAMP、大量历史 Web）。  
> 独立服务（默认 3306）。本仓未定为 Runtime 必需；若业务需要由产品 Core 自管。

## 要点

| 点 | 说明 |
|----|------|
| **模型** | 表 + SQL；InnoDB 事务常见默认 |
| **定位** | 读多写中、Web 业务、存量系统极多 |
| **流行度** | DB-Engines 长期前列（分数见 **版图**） |
| **近亲** | MariaDB 等兼容生态 |

## 与 PostgreSQL 怎么口述

| 维度 | 粗线条（细节以当前版本为准） |
|------|------------------------------|
| 生态惯性 | MySQL 在传统 Web 存量更大 |
| 功能广度 | PostgreSQL 扩展与标准符合度常被夸 |
| 选型 | 团队熟悉度、云托管、扩展需求、合规 |

二者都是 **关系库服务 / 数据中间件**，不是语言（分层见语言章）。

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **InnoDB** | MySQL 最常见的事务型存储引擎，支持行级锁与崩溃恢复 | 新建表默认常是 InnoDB；账务类表几乎必用它 | 别和 **MyISAM**（旧引擎、表级锁为主）混：新业务别再默认选 MyISAM |
| **存储引擎（Storage Engine）** | 真正负责「怎么把行存到磁盘、怎么加锁」的插件层 | 同一 MySQL 实例里不同表可（理论上）用不同引擎 | 引擎 ≠ 数据库产品；换引擎不是换 MySQL |
| **主从复制（Replication）** | 一台写（主），多台读（从），日志同步 | 读多写少时读从库；本仓未内置 MySQL 复制，属运维选型 | 复制延迟 ≠ 事务失败；从库可能读到旧数据 |
| **LAMP**（Linux + Apache + MySQL + PHP） | 经典 Web 技术栈绰号 | 大量历史站点；理解「MySQL 为何存量巨大」 | 不是现代唯一答案；可与 Node/Java 栈并存 |
| **MariaDB** | MySQL 的社区分支，协议与 SQL 高度兼容 | 部分 Linux 发行版默认装 MariaDB；应用常可当 MySQL 用 | 兼容 ≠ 永远 100% 行为一致；升级要看发行说明 |
| **本仓落点** | Runtime **不**强制 MySQL；需要时由产品 Core 自管 | 对照 PostgreSQL 可选 Core 模式；勿发明主服全局 \`mysql\` | 别把「会装 MySQL」当成「本仓必需中间件」 |

## 下一步

**其它常见库** — Oracle、SQL Server、搜索与向量；  
**版图与流行度**；第四章 **数据与缓存**（本仓关系库走可选 Core，非 MySQL 绑定）。  
`;
