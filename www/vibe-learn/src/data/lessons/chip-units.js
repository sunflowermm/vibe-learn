/** 处理单元与存储层次 */
export default `# 处理单元与存储

> 处理单元（\*U）负责计算；存储层次决定「数据离 CPU 有多远」。  
> 二者共同解释性能体感：算得快 ≠ 读得快。

## 本课分块

| 块 | 目标 |
|----|------|
| **处理单元分工** | CPU / GPU / 加速器各擅长什么 |
| **存储层次** | 寄存器 → 缓存 → 内存 → 磁盘 |
| **与 OS / I/O** | 调度、缺页、网卡作为设备 |
| **选型误区** | 后端入门真正依赖什么 |
| **出桥** | 语言章 · 网络章 |

---

## 1. 处理单元分工

\`\`\`match
{"title":"算力角色配对","pairs":[{"id":"cpu","left":"CPU","right":"通用逻辑、分支、系统调用路径"},{"id":"gpu","left":"GPU","right":"大规模同质并行（图形 / 部分 AI）"},{"id":"ram","left":"内存","right":"离 CPU 近的工作集；进程主要舞台"},{"id":"disk","left":"磁盘","right":"持久化；慢但大"}]}
\`\`\`


| 单元 | 擅长 | 直觉 |
|------|------|------|
| **CPU** | 通用逻辑、分支、系统调用路径 | 全能调度与决策 |
| **GPU** | 大规模同质并行（图形、部分 AI） | 同一算式重复极多次 |
| **NPU / TPU 等** | 特定张量/推理负载 | 专用加速 |
| **内存/存储控制器** | 搬移数据 | 后勤通路 |

\`\`\`mermaid
flowchart LR
  P[进程] --> OS[OS 调度]
  OS --> CPU[CPU]
  P -. API .-> DRV[加速器驱动]
  DRV --> GPU[GPU 等]
  CPU <--> RAM[内存]
  GPU <--> VRAM[显存等]
\`\`\`

运行 XRK-AGT 主路径：主要消耗 **CPU + 内存 + 磁盘 + 网卡**。  
本地大模型 / 重图形推理才会强烈依赖 GPU。

---

## 2. 存储层次（关键）

越靠近 CPU：**越快、越小、越贵**；越靠外：**越慢、越大、越便宜**。

\`\`\`steps
{"title":"离 CPU 由近到远","steps":[{"title":"寄存器","body":"CPU 内临时槽位，最快。"},{"title":"Cache","body":"CPU 旁高速缓冲。"},{"title":"内存 RAM","body":"运行中主工作区；断电丢失。"},{"title":"磁盘 SSD/HDD","body":"持久化；慢但大。"}]}
\`\`\`

| 层次 | 典型角色 | 速度量级（直觉） |
|------|----------|------------------|
| **寄存器** | CPU 内临时槽位 | 最快 |
| **Cache（缓存）** | CPU 旁的高速缓冲 | 极快 |
| **内存（RAM）** | 运行中的主工作区 | 快；断电丢失 |
| **SSD / HDD** | 持久化文件与交换 | 慢于内存；持久 |

\`\`\`mermaid
flowchart TB
  REG[寄存器] --> L1[Cache]
  L1 --> RAM[内存 RAM]
  RAM --> DISK[磁盘 SSD/HDD]
\`\`\`

工程含义：

| 现象 | 可能原因 |
|------|----------|
| CPU 不高但很慢 | 频繁读盘、缺页、锁等待 |
| 内存涨满后变卡 | 换页到磁盘，延迟上升 |
| 重启后数据还在 | 已落在持久存储；仅在内存中的会丢 |

程序「打开的文件」「跑着的进程」工作集主要在 **内存**；真正长期保存靠 **磁盘上的文件系统**（见系统的本质 · 文件抽象）。

---

## 3. 与 OS、I/O 的关系

\`\`\`reveal
{"title":"缺页在说什么","prompt":"先读表面，再点开","tone":"info","face":"访问的地址「看起来合法」，却突然卡一下","body":"虚拟地址此刻没有对应物理页时，内核要从磁盘装入（缺页）。伴随停顿；频繁缺页会表现为「CPU 不高但很慢」。网卡收发包也要先进入内存，再交给协议栈与进程。"}
\`\`\`

- OS 将进程调度到 **CPU**；加速器经驱动与专用 API 暴露  
- **缺页**：访问的虚拟地址不在物理内存时，内核从磁盘装入（伴随停顿）  
- **网卡** 属于 I/O 设备：收发包最终仍要进入内存，再交给协议栈与进程  

---

## 4. 选型误区

\`\`\`quiz
{"title":"算力选型快测","questions":[{"q":"跑本仓这类 API / Agent 主服，下列哪句更准确？","choices":[{"t":"必须先买独显，否则 Node 起不来","ok":false,"why":"多数 API 主机可无独显；主路径是 CPU+内存+磁盘+网卡。"},{"t":"主路径吃 CPU / 内存 / 磁盘 / 网卡；GPU 多在本地大模型等场景","ok":true,"why":"与本课「运行 XRK-AGT 主路径」一致。"},{"t":"芯片越新，缺 Node 也能跑","ok":false,"why":"缺运行时/依赖/权限时换芯片无效。"}]}]}
\`\`\`

| 误区 | 更准确 |
|------|--------|
| 有 GPU 即可弱化 CPU | Web、API、数据库、OS 本身大量占用 CPU |
| 服务器必须有独显 | 多数 API 主机可无独显 |
| 芯片越新项目越能跑 | 缺运行时、依赖或权限时，换芯片无效 |

---

## 5. 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **存储层次（Memory Hierarchy）** | 从寄存器、Cache、内存到磁盘，越靠近 CPU 越快但容量越小、每字节越贵。 | 热数据放 Redis/内存，冷归档放对象存储 S3/OSS；DB 索引让热点页留在内存。 | 存储层次 ≠ 只有「内存和硬盘」两层；还有 L1/L2/L3 Cache。 |
| **局部性原理（Locality of Reference）** | 程序刚访问过的数据、或相邻地址，短期内很可能再次被访问。 | 顺序读大文件、循环数组快；随机扫全表慢，因为 Cache 和页缓存都帮不上忙。 | 局部性 ≠ 「数据一定重复」；是统计规律，不是保证。 |
| **Cache 命中 / 未命中（Cache Hit / Miss）** | CPU 要的数据已在某级 Cache 里叫命中；不在则未命中，需从更慢层加载。 | 多线程改同一缓存行不同字段可能「伪共享」，导致 Cache 行来回失效，吞吐暴跌。 | Cache 未命中 ≠ 缺页；前者是 CPU Cache，后者是虚拟内存页。 |
| **缺页异常（Page Fault）** | 访问的虚拟页当前不在物理内存，CPU 陷内核把它从磁盘或 Swap 调入。 | 容器 \`memory limit\` 过小、Java 堆过大，会出现延迟尖刺；监控里 CPU 不高但接口变慢。 | 缺页 ≠ 段错误（Segfault）；合法首次访问也会缺页，非法地址会崩溃。 |
| **交换区 Swap** | 物理内存不够时，OS 把暂时不用的内存页挪到磁盘上的 Swap 分区或文件。 | \`free\` 显示还有空闲但服务极慢，可能在疯狂换页；数据库主机通常建议关掉或限 Swap。 | Swap ≠ 磁盘上的普通日志文件；换页会严重拖慢延迟。 |
| **CPU 密集 vs I/O 密集（CPU-bound vs I/O-bound）** | CPU 密集任务大部分时间在做运算；I/O 密集任务大部分在等磁盘、网络等外设。 | 视频转码、加密签名要加核；大量小文件上传、报表导出要加磁盘带宽或异步队列。 | CPU 占用 100% 不一定是 CPU 密集——也可能是忙等锁；要看等的是什么。 |
| **GPU（Graphics Processing Unit，图形/通用并行处理器）** | 拥有大量小核心，擅长同一指令对海量数据并行计算。 | AI 训练推理、图像渲染用 GPU；普通 CRUD API、JSON 解析仍以 CPU 为主，上 GPU 往往得不偿失。 | GPU ≠ 「更快的 CPU」；分支多、串行逻辑强的工作不适合 GPU。 |
| **ALU / 控制单元（Arithmetic Logic Unit / Control Unit）** | ALU 做算术逻辑运算，控制单元指挥取指、译码、执行的节奏。 | 单线程性能看主频与 IPC；编译优化让同样代码少做无效指令，就是在帮 ALU 省活。 | ALU 在 CPU 核内；GPU 里的是大量简化核，不是同一类 ALU 设计。 |

---

## 6. 下一步

- **第二章 · 计算机语言** — 源码如何变成 CPU 上的指令流  
- **第三章 · 计算机网络** — 多机经网卡与协议栈对话  
- 回看 **软硬件联动** — 中断与 I/O 如何喂饱本课的单元与存储  
`;
