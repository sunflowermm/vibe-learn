export default `# SQLite

> **分类：嵌入式关系型 DBMS**——库以 **文件**（或内存）形式存在，通常 **链进应用进程**，不默认另起服务器端口。  
> 仍是完整 DBMS（SQL、事务、索引），只是部署形态与 MySQL/PG 不同。  
> 本课讲 **形态与适用**；本仓 \`node:sqlite\` / 全局名 → 第四章 **数据与缓存**。  
> **学会之后**：能画「应用进程内嵌 SQLite → 文件」图，并说明为何不能替代 Redis 热缓存。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 形态 | 嵌入式文件库；通常无监听端口 |
| 仍是 DBMS | 有 SQL/事务/索引；不是「随便一个 json 文件」 |
| 适用 | 本机状态、测试、边缘；高并发多写中心库慎用 |
| 本仓 | Runtime 本地持久常见；热缓存仍归 Redis |

## 本课你要带走什么

1. 嵌入式 vs 客户端–服务器  
2. 适合 / 不适合什么  
3. 为何 **不能** 用它替代 Redis 热缓存

---

## 1. 形态对比

\`\`\`flip
{"title":"SQLite · 翻卡","cards":[{"front":"SQLite","back":"文件型库，常嵌在进程里，少独立服务"},{"front":"别混成","back":"编程语言 / Web 框架"},{"front":"选型先问","back":"模型、一致性、运维与生态"}]}
\`\`\`

| | **SQLite** | **PostgreSQL / MySQL** |
|--|------------|-------------------------|
| 进程 | 多在应用内 | 独立服务进程 |
| 网络 | 通常无监听端口 | 有端口 |
| 存储 | 单个（或少数）库文件 | 服务端数据目录 |
| 并发写 | 单写者模型要心里有数 | 面向多连接并发设计 |
| 运维 | 几乎「无服务可运维」 | 要备份、用户、调优 |

\`\`\`mermaid
flowchart LR
  App[应用进程] --> Lib[SQLite 库代码]
  Lib --> File[(*.sqlite 文件)]
\`\`\`

服务形态总论见 **数据库服务**（本产品是「嵌入例外」）。

---

## 2. 何时用

| 适合 | 不太适合作唯一库 |
|------|------------------|
| 本机状态、嵌入设备、桌面/单机工具 | 超高并发多写中心库 |
| 测试、原型、边缘节点 | 需要复杂复制拓扑的全球主库（可另有方案） |
| Runtime 本地持久（本仓用法） | 替代 Redis 热缓存（勿混） |

## 和 Agent 原型的交界

单机会话状态、实验性记忆表、本地评测结果——SQLite 很合适。  
多租户生产、复杂权限与高并发写——尽快上 **数据库服务**（PG 等），并搞清连接池与备份。

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **嵌入式 DBMS（Embedded DBMS）** | 数据库引擎作为库链进应用进程 | SQLite 读写本地 \`.sqlite\` 文件 | 别和 **C/S（Client–Server）DBMS** 混：后者独立监听端口 |
| **C/S 架构（Client–Server）** | 应用通过网络连独立数据库服务 | App → PostgreSQL:5432 | SQLite 默认无此形态 |
| **单写者模型（Single-Writer）** | 同一时刻写入串行化 | SQLite 高并发写要评估 | 别和 PG「多连接并发写」预期混 |
| **WAL（Write-Ahead Logging，预写日志）** | 先写日志再改页，崩溃可恢复 | SQLite 默认 WAL 模式 | 与 Redis AOF 概念类似，产品不同 |
| **零运维进程（Zero Server Process）** | 无需另起 \`sqlite-server\` | 本仓 Runtime 本地持久 | 别因此说「不是 DBMS」——仍是完整 DBMS |
| **关系模型 + SQL** | 表、索引、事务一应俱全 | 单机工具、测试、边缘节点 | 别用来替代 Redis 热缓存职责 |

## 下一步

**Redis** — 热数据对面；  
**MongoDB / PostgreSQL** — 可选业务库形态；  
第四章 **数据与缓存** — 本仓契约。
## 导图2 · 后端 / 数据库 × SQLite

> 嵌入式库；本仓热路径另一极。与「作为服务」对照。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **数据库** | 文件型嵌入式 | 常无独立服务器进程 |
| **后端** | 同进程库 | 本仓 Runtime 使用场景见第四章 |
| **部署上线** | 文件与权限 | 备份要含库文件 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
