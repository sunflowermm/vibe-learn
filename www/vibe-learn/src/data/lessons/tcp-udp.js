/** TCP / UDP — 对齐课件 */
export default `# TCP 与 UDP

> 传输层负责**进程到进程**的通信：用**端口**区分同一主机上的不同程序，并选择可靠或尽力而为的传送方式。

## 知识串

| 上一层问题 | 本课回答 |
|------------|----------|
| **IP** 找到哪台电脑 | **端口**找到哪个程序 |
| 序章的 **套接字** | 传输层协议（TCP/UDP）常通过 socket API 使用 |
| 后面的 HTTP | 多数跑在 **TCP** 上；DNS 查询常用 **UDP** |

记忆钩：**IP 找主机，端口找进程；TCP 挂号信，UDP 明信片。**

## 和调模型 API 的关系

| 步骤 | 协议直觉 |
|------|----------|
| \`api.xxx.com\` | 先 **DNS** |
| 443 | 常见 **HTTPS** 端口 |
| TLS | 加密与证书校验 |
| 请求体 JSON | 跑在 **TCP** 连接上的 **HTTP** |

丢包、乱序、半包——TCP 帮你扛；你业务层仍要处理 **超时、429、重试退避**。

\`\`\`match
{"title":"TCP 与 UDP 对照配对","pairs":[{"id":"tcp","left":"TCP","right":"可靠、有序、面向连接（挂号信）"},{"id":"udp","left":"UDP","right":"尽力而为、无连接（明信片）"},{"id":"http","left":"多数 HTTP","right":"跑在 TCP 上"},{"id":"dns","left":"DNS 查询（常见）","right":"常用 UDP"}]}
\`\`\`

\`\`\`sort
{"title":"排出三次握手顺序","caption":"从上到下应为第 1→3 步","items":[{"id":"a","text":"客户端 → 服务器：SYN","order":0},{"id":"b","text":"服务器 → 客户端：SYN-ACK","order":1},{"id":"c","text":"客户端 → 服务器：ACK","order":2}]}
\`\`\`


## TCP（传输控制协议）

特点可以记四条：

| 特性 | 含义 |
|------|------|
| **可靠** | 尽量保证完整到达，丢了可重传 |
| **有序** | 按序号拼回正确顺序 |
| **面向连接** | 传数据前先建立连接 |
| **流量控制** | 避免发得比对方收得还快 |

### 三次握手（建立连接）

1. 客户端 → 服务器：\`SYN\`（我想连）  
2. 服务器 → 客户端：\`SYN-ACK\`（好，我也准备好了）  
3. 客户端 → 服务器：\`ACK\`（确认）  

之后才开始正常传数据。  
入门先记住：**可靠连接不是免费的，要先打招呼**。

### 四次挥手（断开，常考）

1. 主动方 → 被动方：\`FIN\`（我发完了）  
2. 被动方 → 主动方：\`ACK\`（知道了）  
3. 被动方 → 主动方：\`FIN\`（我也发完了）  
4. 主动方 → 被动方：\`ACK\`  

为何常说「四次」：TCP 半关闭——一方不再发送，另一方可能还有数据要发。  
**TIME_WAIT**：主动关闭方在最后 ACK 后短暂停留，吸收迟到包、保证对端可靠关闭；高并发短连接时端口占用是常见面试题。

### 可靠与「别发太猛」（名称级）

| 机制 | 直觉 |
|------|------|
| **序号 / 确认号** | 丢了能重传、乱了能重排 |
| **滑动窗口** | 未确认前能在途发多少 |
| **流量控制** | 接收方窗口告诉发送方「我吃得下多少」 |
| **拥塞控制** | 网络忙时主动降速（慢启动、拥塞避免等名常考） |

### TCP 常见场景

网页（HTTP/HTTPS）、邮件（SMTP）、文件传输（FTP）、多数聊天与 API——**错一点都不舒服**的业务。

## UDP（用户数据报协议）

| 特性 | 含义 |
|------|------|
| **快** | 不先握手，直接发 |
| **无连接 / 无状态** | 不维护复杂连接状态 |
| **不可靠** | 可能丢、乱序，自己要在应用层决定怎么处理 |
| **低延迟倾向** | 适合实时场景 |

### UDP 常见场景

在线游戏、直播、VoIP、很多 DNS 查询——**晚一点不如丢一点**的业务。  
现代 Web 还有 **QUIC / HTTP/3**：在 UDP 上自己做可靠与多路复用（名常考）。

### 实务：开 Minecraft 服务器时，安全组开错协议就连不上

云主机（阿里云 / AWS 安全组、腾讯云防火墙等）规则是 **协议 + 端口**，不是「只写一个数字」。

| 版本 | 默认端口 | 必须放行的协议 | 常见翻车 |
|------|----------|----------------|----------|
| **Java 版** | \`25565\`（可改） | **TCP** | 只开了 UDP，或只开了端口忘选 TCP |
| **基岩版 Bedrock** | \`19132\`（IPv4；IPv6 常 \`19133\`） | **UDP** | 只开了 TCP 25565，基岩客户端永远进不去 |
| **Geyser 等桥接** | Java TCP + 基岩 UDP 常同时要 | **两套都要** | Java 能进、手机基岩进不去 → 查 UDP 19132 |

\`\`\`
玩家 ──TCP:25565──► 云安全组放行 TCP 25565 ──► Java 服务进程
玩家 ──UDP:19132──► 云安全组放行 UDP 19132 ──► Bedrock / Geyser
\`\`\`

对照本课与 **路由 / NAT**：

1. 进程在听哪个端口、用 **TCP 还是 UDP**（\`ss -ulnp\` / \`netstat\`）  
2. 云 **安全组 / ACL** 是否按**同一协议**放行入站  
3. 家用还要 **端口转发** 到内网机（协议选错同样失败）  
4. 本机防火墙（Windows 防火墙、\`ufw\`）也要一致  

记忆：**端口数字对了不够，协议也必须对。** 网页开 \`443/TCP\`；基岩开服开 \`19132/UDP\`。

\`\`\`quiz
{"title":"TCP / UDP 自测","questions":[{"q":"基岩版默认 19132，安全组只开 TCP 19132？","choices":[{"t":"通常仍进不去；基岩要 UDP，协议字段必须对","ok":true,"why":"Java 版才是常见 TCP 25565。"},{"t":"TCP/UDP 在安全组完全等价","ok":false,"why":"开错协议等于没开。"},{"t":"只要 DNS 通就能进","ok":false,"why":"解析≠传输放行。"},{"t":"基岩只走 ICMP","ok":false,"why":"走 UDP 端口。"}]},{"q":"TCP 粘包更准确的说法？","choices":[{"t":"字节流无消息边界，应用要自己定帧","ok":true,"why":"不是内核随机把包粘坏。"},{"t":"只有 UDP 会粘包","ok":false,"why":"说反了。"},{"t":"开 HTTPS 后自动消失","ok":false,"why":"TLS 不改字节流本质。"},{"t":"等于网线短路","ok":false,"why":"协议语义，不是硬件故障。"}]}]}
\`\`\`

## TCP vs UDP 对照

| | TCP | UDP |
|--|-----|-----|
| 连接 | 要握手 | 通常不要 |
| 可靠 | 强 | 弱（尽力而为） |
| 速度 / 延迟 | 相对重 | 相对轻 |
| 比喻 | 挂号信 | 明信片 |

## 端口再强调

| 词 | 含义 |
|----|------|
| **端口** | 0–65535 的数字隔间，区分同机不同进程 |
| **监听 listen** | 服务进程占用某端口等待连接（环境章跑起的服务就在听端口） |
| 常见约定 | \`80\` HTTP、\`443\` HTTPS、\`53\` DNS（习惯，不是物理插孔） |
| **知名 / 注册 / 动态端口** | 0–1023 常留给系统服务；客户端源端口多为高位临时端口 |

---

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **三次握手 / 四次挥手（TCP 3-way Handshake / 4-way Teardown）** | 建连时 SYN→SYN-ACK→ACK 确认双方都能收发；断连时 FIN/ACK 双方各自关闭方向。 | 连不上服务先看握手是否完成（SYN 重传、半连接堆积）；连接泄漏感常是挥手不完整或客户端未 close。 | 握手失败 ≠ 应用返回 401；TCP 层没建连，HTTP 根本发不出去。 |
| **TIME_WAIT** | 主动关闭连接的一方在拆连后会保留 socket 一段时间（常见 2MSL），防止迟到的旧包干扰新连接。 | 短连接压测打满临时端口、出现 \`Cannot assign requested address\`；可改长连接、连接池或调内核参数。 | TIME_WAIT 不是「内存泄漏」；是协议规定的正常状态，数量多时要优化连接模式。 |
| **SYN 洪水（SYN Flood）** | 攻击者发大量 SYN 却不完成握手，占满半连接队列， legit 用户建连失败。 | 需 SYN Cookie、防火墙限速、云 WAF/抗 D 服务；自建服务要限制 \`somaxconn\` 并监控半连接数。 | SYN 洪水 ≠ 正常流量高峰；后者完成握手并有真实数据传输。 |
| **Keep-Alive（TCP/HTTP 连接保活）** | 复用已建立的 TCP 连接发多个请求，避免每次重新三次握手。 | HTTP/1.1 默认长连接；数据库连接池、Redis 连接池都是同一思想，省 RTT 与 CPU。 | HTTP Keep-Alive ≠ TCP Keepalive 探测包；后者是检测死连接，语义不同。 |
| **粘包 / 拆包（TCP Message Boundary）** | TCP 把数据当连续字节流，不保留「消息边界」，应用要自己定帧格式。 | 自定义二进制协议要用定长头、分隔符或长度前缀；直接用 HTTP/JSON 则框架已帮你分帧。 | 粘包不是 bug；是 TCP 设计如此。UDP 按 datagram 边界，但可能丢包乱序。 |
| **TCP vs UDP 选型** | TCP 保证可靠有序、有拥塞控制；UDP 不保证到达，但延迟低、头部小。 | 订单、支付、文件下载用 TCP；实时语音、游戏状态、QUIC 底层用 UDP 再自己做可靠层。 | 「可靠」不是只有 TCP；QUIC 在 UDP 上也能做可靠传输。 |
| **安全组协议字段** | 防火墙规则里除了 IP/端口，还要写 TCP、UDP 或 ICMP 等传输层协议。 | 我的世界基岩版须开 **UDP 19132**；只开 TCP 19132 等于端口仍「没开」。 | 安全组 ≠ 应用层鉴权；端口开了仍要登录态、Token 等。 |
| **QUIC（Quick UDP Internet Connections）** | 在 UDP 之上实现多路复用、加密、可靠传输的现代传输协议，HTTP/3 跑在它上面。 | Chrome 访问很多站点用 HTTP/3，抓包看到 **UDP 443** 是正常的；弱网切换网络时连接迁移更快。 | QUIC ≠ 「裸 UDP 业务协议」；它内部仍有 TLS 1.3 与流控，只是不用 TCP。 |

## 下一步

**路由与 NAT**（出网关、安全组）· **DNS/HTTPS**（名字与加密）· **HTTP**（应用层说话）· 番外 **Clash 端口**（本机代理也在 listen）。
`;
