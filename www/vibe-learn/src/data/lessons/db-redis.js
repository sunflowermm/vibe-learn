export default `# Redis

> **分类：内存为主的数据结构服务器 / 键值型 DBMS**；工程上常称 **缓存与协调中间件**。  
> **不是**编程语言，**不是** Node / Express 框架。  
> 本课只讲 **产品是什么**；本仓路径与 fail-fast → 第四章 **数据与缓存**；安装与 PATH → **部署环境**。  
> **学会之后**：能口述 Redis 模型与典型用途，并分清穿透/击穿/雪崩直觉。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 分类 | 内存数据结构服务器 / 键值 DBMS；不是语言或框架 |
| 用途 | 缓存、会话、限流、锁中至少说出两项 |
| 对照 | 热数据→Redis；本机落盘→SQLite；复杂 SQL→PG/MySQL |
| 本仓 | 契约与 fail-fast 在第四章；本课不讲 yaml 路径 |

\`\`\`algo
{"kind":"dbtier","title":"热数据 Redis · 落盘 SQLite","autoplay":true,"speed":860}
\`\`\`

\`\`\`check
{"title":"Redis 通关","items":[{"id":"what","text":"能说明：内存数据结构服务器 / 键值 DBMS，不是语言","hint":"分类"},{"id":"use","text":"能举缓存/会话/限流/锁中至少两项","hint":"用途"},{"id":"vs","text":"能对照：热→Redis；落盘→SQLite；复杂 SQL→PG/MySQL","hint":"职责"}]}
\`\`\`

## 本课你要带走什么

1. Redis 存的是什么（结构类型）  
2. 为何几乎总是「一个服务进程」  
3. 和 SQLite / 关系库各管什么（一句话）

---

## 1. 它是什么

| 点 | 说明 |
|----|------|
| **模型** | 键 → 字符串 / Hash / List / Set / ZSet / Stream… |
| **速度** | 数据主要在内存；可配置持久化（RDB/AOF） |
| **访问** | 客户端经 TCP（默认 6379）发命令 |
| **典型用途** | 缓存、会话、限流计数、分布式锁、轻量队列、发布订阅 |

\`\`\`mermaid
flowchart LR
  App[业务进程] -->|命令| Redis[redis-server]
  Redis --> Mem[(内存结构)]
  Redis -.-> Disk[(可选持久化)]
\`\`\`

---

## 2. 为何算中间件 / 服务

- 独立安装与进程（或云 Redis）  
- 多个应用实例共享同一逻辑缓存  
- 应用只持有客户端，不「编译进 Redis」  

定义总表回 **中间件视角** / 语言章 **库 · 框架 · 运行时 · 中间件**。

---

## 3. 和谁对照（别混职责）

| 需求 | 更常看 |
|------|--------|
| 热数据、秒级过期、跨实例共享状态 | **Redis** |
| 本机结构化落盘、少运维进程 | **SQLite** |
| 复杂 SQL / 强事务业务表 | **PostgreSQL / MySQL…** |

流行度分数见 **版图与流行度**（Redis 分数低于巨型关系库，但缓存场景近乎标配）。

### 动手：假 redis-cli

> 不上网、不连真实 Redis；自动演示 \`ping → PONG\`。

\`\`\`shell
{"preset":"redis-ping"}
\`\`\`

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **Redis（Remote Dictionary Server）** | 内存为主的数据结构服务器/键值 DBMS | 缓存、会话、限流、分布式锁 | 别和 **Memcached** 或「纯字符串缓存」画等号：类型更丰富 |
| **RDB（Redis Database Backup，快照持久化）** | 定时把内存快照写到磁盘 | 重启恢复快；可能丢最近几秒 | 别和 **AOF** 混：RDB 是整点快照，AOF 是命令日志 |
| **AOF（Append Only File，追加日志持久化）** | 每条写命令追加到日志文件 | 可配置 fsync 策略换 durability | 文件可能比 RDB 大；恢复常更细 |
| **缓存穿透（Cache Penetration）** | 查不存在的数据，缓存拦不住直打 DB | 恶意扫 id、布隆过滤器 | 别和 **击穿** 混：击穿是热点 key 过期 |
| **缓存击穿（Cache Breakdown）** | 热点 key 过期瞬间并发打穿 DB | 秒杀商品 key 失效 | 别和 **雪崩** 混：雪崩是大面积 key 同时失效 |
| **缓存雪崩（Cache Avalanche）** | 大量 key 同时过期或 Redis 宕机 | 流量洪峰打垮 DB | 需要 TTL 随机化、集群高可用 |
| **键值模型（Key-Value Model）** | key → 字符串/哈希/列表等结构 | \`SET user:1 session\` | 别当关系表用：复杂 JOIN 不擅长 |

## 本仓怎么做

| 概念 | 落点 |
|------|------|
| 热缓存 / 会话 / 限流 | Runtime **必需** Redis；fail-fast |
| 对照 | 落盘 → SQLite；复杂 SQL → 可选 PG/MySQL Core |
| 契约 | 第四章 **数据与缓存** · \`docs/database.md\`；装机 → **部署环境** |

## 下一步

**SQLite** — 嵌入式对照；  
第四章 **数据与缓存** — 本仓必需与用法。
`;
