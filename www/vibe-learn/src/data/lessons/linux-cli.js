/** Linux 基础指令 */
export default `# Linux 基础指令

> 不求背诵手册，先建立 **空间感**：我在哪、有什么、怎么看、怎么动。  
> 下列在 bash/zsh 下通用；PowerShell 有对应物但语法不同。  
> 本课补上日常开发极高频的 **curl / wget** 等「从网上拿东西」的命令。

## 知识串

| 已学 | 本课动手 |
|------|----------|
| **终端 / Shell** | 在窗口里敲这些命令 |
| **文件系统** | \`cd\` / \`ls\` 在目录树里走动 |
| **进程** | \`ps\` / \`kill\` 看见并结束进程 |
| **权限 / sudo** | 为何有时 Permission denied |
| **PATH / 包管理** | \`curl\` 本身也是装进 PATH 的程序 |

\`\`\`mermaid
flowchart LR
  A["我在哪 pwd"] --> B[有什么 ls]
  B --> C[去哪里 cd]
  C --> D["看内容 / 改文件"]
  D --> E["看进程 / 权限"]
  E --> F["网络：curl / wget"]
\`\`\`

## 本课分块

| 块 | 内容 |
|----|------|
| **模拟终端** | 直接输入命令，看虚拟结果 |
| **导航与感知** | pwd / ls / cd |
| **读写与查找** | cat / mkdir / grep / find |
| **进程与权限** | ps / kill / chmod / sudo |
| **网络小工具** | curl / wget / ping |
| **接到本仓** | 进入工作区并验证工具链 |

---

## 0. 先动手：模拟终端

> **全部是假的**：不上网、不改磁盘；点芯片会**自动打字并执行**；也可手敲。  
> 面板底部还有完整沙箱。一课可以有多个终端窗。

\`\`\`shell
{"preset":"linux-cli"}
\`\`\`

建议自己再试：\`cd projects/XRK-AGT\` → \`cat package.json\` → \`help\`

---

## 1. 导航与感知

| 命令 | 作用 | 零基础备注 |
|------|------|------------|
| \`pwd\` | 打印当前目录 | 「我站在哪」 |
| \`ls\` / \`ls -la\` | 列出文件 | \`-la\` 含**点开头隐藏文件**；深挖见番外 **点文件与隐藏项** |
| \`cd 路径\` | 进入目录 | \`cd ..\` 回上层；\`cd ~\` 回家目录（地图见番外 **本机目录地图**） |
| \`tree\`（若已装） | 树状查看 | 没有就先用 \`ls\` |

路径两种直觉：

- **绝对路径**：从根 \`/\`（或盘符）写起  
- **相对路径**：从「你现在站的地方」写起  

> 只读回放（对比上面可输入沙箱）：

\`\`\`term
{"title":"导航三连回放","prompt":"$ ","steps":[{"type":"in","text":"pwd"},{"type":"out","text":"/home/alice"},{"type":"in","text":"ls -la"},{"type":"out","text":"drwx------  .\\ndrwxr-xr-x  ..\\n-rw-------  .bashrc\\ndrwxr-xr-x  Documents"},{"type":"in","text":"cd ~/projects && pwd"},{"type":"out","text":"/home/alice/projects"}]}
\`\`\`

## 2. 读写与查找

| 命令 | 作用 |
|------|------|
| \`cat\` / \`less\` | 看文件内容（\`less\` 可翻页） |
| \`mkdir\` / \`rm\` / \`cp\` / \`mv\` | 建目录、删、拷、移 |
| \`grep\` | 在文本里搜关键字 |
| \`find\` | 按名/条件找文件 |

<details>
<summary>展开：危险操作的最小礼貌</summary>

- 先 \`ls\` 再 \`rm\`；不确定就先 \`mv\` 到临时目录  
- \`rm -rf /\` 类命令不要「试手气」  
- 生产服务器上的 \`chmod 777\` 几乎总是错误捷径  
</details>

## 3. 进程与权限（常踩坑）

| 命令 | 作用 |
|------|------|
| \`ps\` / \`top\` / \`htop\` | 看进程 |
| \`kill\` / \`kill -9\` | 结束进程（先礼后兵；\`-9\` 强杀） |
| \`chmod\` / \`chown\` | 改权限 / 所有者 |
| \`sudo\` | 临时提升权限执行 |

这些命令直接碰到 **系统的本质** 里讲的权限与进程抽象。  
看见 \`Permission denied\`：先想「我是谁、文件属谁」，再决定要不要 \`sudo\`。

## 4. 网络小工具：curl / wget / ping

| 命令 | 全称直觉 | 干什么 |
|------|----------|--------|
| **curl** | Client URL | 用命令行发 HTTP(S) 等请求：看响应、下文件、测 API |
| **wget** | WWW get | 偏「把远程文件下载到磁盘」；镜像站点也常用 |
| **ping** | 发包探测 | 测主机是否大致可达（ICMP）；**不等于**「网站一定能开」 |

\`\`\`bash
# 看某个 URL 返回什么（常用排障）
curl -I https://example.com

# 下载文件到当前目录
curl -LO https://example.com/file.tar.gz
# 或
wget https://example.com/file.tar.gz

# 本机健康检查（主服起来后）
curl -s http://127.0.0.1:端口/api/health
\`\`\`

**curl 是什么？**  
一个装在系统里的 **命令行客户端**（可用 apt/brew 安装），不是编程语言，也不是浏览器。  
脚本、CI、文档里的「打一下这个 API」几乎都写 curl。Windows 10+ 常自带 curl.exe；没有就用浏览器或 \`Invoke-WebRequest\`。

和第三章 **HTTP**、第四章 **Auth / health** 直接相关：你会反复用它验证服务活着没有。

## 5. 和本仓库的关系

\`\`\`bash
cd XRK-AGT          # 进入工作区
ls                  # 应能看到 package.json
node -v && pnpm -v  # 验证工具链
\`\`\`

指令是「手」；**第一章其它卡片**提供「装哪只手、站在哪片地」。

## Windows 用户怎么办？

| 你的环境 | 建议 |
|----------|------|
| PowerShell | 部分命令不同（\`ls\` 可用别名；\`pwd\` 也有）；可用 \`curl.exe\` 避免别名坑 |
| WSL / Git Bash | 上面表格基本可直接练 |
| 纯 cmd | 建议换 Windows Terminal + PowerShell 或 WSL |

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **curl**（Client URL） | 命令行网络客户端，常发 HTTP 请求 | \`curl /api/health\`、下载安装脚本 | 别和「浏览器」或「爬虫框架」等同 |
| **wget** | 偏下载/镜像的命令行工具 | \`wget\` 拉压缩包 | 和 curl 功能重叠；团队文档写哪个跟哪个 |
| **HTTP 状态码** | 服务器用数字说结果 | curl \`-I\` 看 200/404/502 | 状态码 ≠ ping 通 |
| **shebang（\`#!\`）** | 脚本第一行指定用哪个解释器 | \`#!/usr/bin/env bash\` | 不是「魔法注释」无关紧要 |

## 下一步

**Git 与工作区** — 用 \`cd\` / \`ls\` 确认你站在仓库根，再谈 clone 与托管平台。  
番外 **本机目录地图** — Users / AppData / \`/bin\` / \`/home\`；**点文件与隐藏项** — \`.env\` \`.git\`。  
番外 **容器** — 容器端口映射后可用 curl 探测；  
第三章 **Nginx** — 门面入口也常用 curl 验。  
`;
