export default `# 容器是什么

> **容器（Container）** ≈ 把「应用 + 它依赖的库/配置」打成可重复运行的单元，在宿主机内核上以隔离进程方式跑起来。  
> **不是**一门语言，**不是** Web 框架，也**不是**完整虚拟机（通常不自带另一套完整客户机 OS 内核）。  
> **学会之后**：能对照容器 vs VM，并用镜像/容器/仓库三件套解释一次「起 Redis」。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 痛点 | 环境一致、依赖进镜像、少污染宿主机 |
| vs VM | 共享宿主机内核 vs 客户机自带内核 |
| 三件套 | Image / Container / Registry 不混称 |
| 装环境 | 与 apt、pnpm 分层，不互相替代 |

## 本课分块

| 块 | 目标 |
|----|------|
| **要解决什么** | 「我机子能跑、你机子不行」 |
| **容器 vs 虚拟机** | 共享内核 vs 模拟整机 |
| **镜像 / 容器 / 仓库** | 三件套名词 |
| **和本仓直觉** | 为何常拿 Docker 起 Redis |

---

## 1. 要解决什么痛

| 痛点 | 容器思路 |
|------|----------|
| 环境不一致 | 同一镜像，开发/测试/生产尽量同配方 |
| 「依赖装炸系统」 | 依赖进镜像，少污染宿主机全局 |
| 起一组服务麻烦 | 再叠加 Compose：多容器一键编排 |

\`\`\`mermaid
flowchart LR
  Img[镜像 Image<br/>只读配方] --> Run[容器 Container<br/>运行中的实例]
  Run --> Host[宿主机内核 · 网络 · 磁盘]
\`\`\`

---

## 2. 容器 vs 虚拟机（白话）

| | **容器** | **虚拟机 VM** |
|--|----------|----------------|
| 内核 | 多共享**宿主机内核** | 客户机常有**自己的内核** |
| 重量 | 相对轻、启动快 | 相对重 |
| 隔离 | 进程 / 命名空间 / cgroup 级 | 硬件虚拟化级（更「整机」） |
| 典型用途 | 微服务、中间件、CI 任务 | 完整桌面/另一套 OS、强隔离需求 |

两者可并存：云主机（VM）里再跑 Docker（容器）很常见。

---

## 3. 三件套

| 名词 | 白话 |
|------|------|
| **镜像（Image）** | 只读模板：里面有文件系统快照与启动命令 |
| **容器（Container）** | 镜像跑起来的实例（可起停、可删） |
| **仓库（Registry）** | 存/拉镜像的地方（Docker Hub、私有仓…） |

类比（不严谨但好记）：镜像 ≈ 安装包配方；容器 ≈ 正在跑的进程组；仓库 ≈ 应用商店。

> 先摸一下「镜像 / 正在跑的容器」长什么样（假输出；下一课 Docker 细讲）。

\`\`\`shell
{"preset":"docker-basics"}
\`\`\`

---

## 4. 和语言包管理、系统包管理的边界

| 手段 | 装到哪 |
|------|--------|
| **apt / brew** | 宿主机系统里 |
| **pnpm / pip** | 项目依赖目录 |
| **容器镜像** | 镜像文件系统里（与宿主机隔离） |

第一章 **包管理器**、**安装器与 PATH** 讲前两种；本框讲第三种。  
**Nginx / 反代** 不在本框——见第三章。

### 对照卡：三种「装环境」

\`\`\`compare
{"title":"装到哪？三种手段","caption":"角色不同，别互相替代。","items":[{"role":"系统包管理","win":"winget / scoop","linux":"apt / dnf","mac":"Homebrew","note":"装进宿主机 PATH"},{"role":"语言包管理","win":"pnpm（本仓）","linux":"pnpm / uv…","mac":"同左","note":"项目依赖，不换 OS"},{"role":"容器镜像","win":"Docker Desktop","linux":"Docker / Podman","mac":"Docker Desktop","note":"依赖进镜像，少污染宿主机"}]}
\`\`\`

下一课 **Docker** 有可输入假终端（\`docker ps\` 等）。

## 八股 × 业务串联

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **Container（容器）** | 共享宿主机内核、以命名空间/cgroup 隔离的进程运行单元 | 一个 Redis 容器监听 6379 | 不是完整虚拟机；通常不自带另一套内核 |
| **Image（镜像）** | 只读层叠文件系统 + 启动元数据模板 | \`redis:7\` 镜像可起多个容器实例 | 镜像 ≠ 正在运行的进程 |
| **Registry（镜像仓库）** | 存储与分发镜像的远程仓库 | Docker Hub、私有仓 | 别和 npm registry 混：对象不同 |
| **Namespace / cgroup** | Linux 隔离进程视图与资源限额的机制 | 容器「看不见」宿主机其它进程的网络栈（默认） | 是内核能力，不是 Docker 商标专属 |

## 下一步

**Docker** — 最常见的容器引擎怎么用（概念级）。
## 导图2 · 部署 / 构建 / 后端 × 容器是什么

> 导图2 部署口语；本课钉镜像/容器/仓库三件套。≠ Nginx 门面。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **部署上线** | 可搬运环境 | 容器共享内核，不是完整 VM |
| **构建** | 镜像配方 | Image ≠ 正在跑的进程 |
| **后端** | 进程怎么跑 | 常与 Compose 起依赖 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
