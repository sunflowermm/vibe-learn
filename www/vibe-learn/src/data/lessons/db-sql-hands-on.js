/** SQL 最小动手 */
export default "# SQL 最小动手\n\n> 库产品很多；零基础先会 **CRUD 四句**，再回 Redis/SQLite 分课。\n\n## 四句骨架\n\n```sql\nSELECT id, name FROM users WHERE id = 1;\nINSERT INTO users (name) VALUES ('aya');\nUPDATE users SET name = 'bob' WHERE id = 1;\nDELETE FROM users WHERE id = 1;\n```\n\n```flip\n{\"title\":\"SQL 翻卡\",\"cards\":[{\"front\":\"表\",\"back\":\"行的集合；列有类型\"},{\"front\":\"主键\",\"back\":\"唯一定位一行\"},{\"front\":\"WHERE\",\"back\":\"过滤行；更新/删除务必带条件\"}]}\n```\n\n## 建议练习\n\n1. 安装/打开任意 SQLite 客户端（或 `sqlite3` CLI）\n2. 建表 `notes(id INTEGER PRIMARY KEY, body TEXT)`\n3. 插入两行，`SELECT` 出来\n4. 更新一行，再删一行\n\n## 接到本仓\n\n- **SQLite / Redis** 契约见数据库番外与 `xrk-database`\n- 业务里谁读写、配置落在哪，回第四章\n\n## 下一步\n\n**SQLite** 或 **Redis** 产品课。\n";
