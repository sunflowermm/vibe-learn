/** 向量库：索引与近邻查询（工程向） */
export default `# 向量库

> **本课位置**：嵌入之后的**存储与查询**层。  
> **先修**：Embedding 直觉、分块策略。  
> **要点**：向量库 = 近邻索引 + 元数据；换嵌入模型通常要整库重嵌。  
> **对照**：传统数据库按等值/范围查行；向量库按「离查询向量有多近」查邻居——和菜鸟教程里「IP 负责把包送到地址」类似：这里「地址」是高维空间里的位置。

## 定义

**向量库 / 向量索引**：存高维向量，按相似度返回近邻；常带 payload（文档 id、权限、时间、原文引用）。

| 概念 | 白话 |
|------|------|
| 维度 | 与嵌入模型绑定，同库须一致 |
| 度量 | 余弦 / 内积 / L2 等；建库与查询要一致 |
| ANN | 近似最近邻：用少许召回损失换速度 |
| HNSW 等 | 常见图索引；吃内存、查询快 |
| 过滤 | 租户 / ACL / 时间窗与近邻组合 |

## 和「普通数据库」怎么分工

| | 关系库 / 文档库 | 向量库 |
|--|-----------------|--------|
| 擅长 | 精确字段、事务、报表 | 语义相似 |
| 权限 | 行级/集合级成熟 | 必须显式做 metadata 过滤 |
| 变更 | 改一行即可 | 改嵌入模型 → 常要重嵌 |

实务：**元数据仍可放 PG/对象存储**；向量库存向量 + 回指 id。\`pgvector\` 则是「一库两用」。

\`\`\`mermaid
flowchart LR
  DOC[文档] --> CHUNK[分块]
  CHUNK --> EMB[同一嵌入模型]
  EMB --> VS[向量索引]
  Q[问题] --> EMB2[同一模型]
  EMB2 --> VS
  VS --> HITS[Top-K + 过滤]
  HITS --> LLM[拼进提示]
\`\`\`

## 选型直觉（不必背品牌）

| 需求 | 倾向 |
|------|------|
| 本地原型 | FAISS / Chroma / 内存索引 |
| 已有 Postgres | pgvector |
| 大规模多租户 | 专用向量服务 + 分区 / 过滤 |
| 强合规 | 自托管 + 加密与审计 |

\`\`\`quiz
{"title":"向量库","questions":[{"q":"换了嵌入模型却不重建索引，通常会？","choices":[{"t":"自动对齐，无影响","ok":false,"why":"空间不同，近邻无意义。"},{"t":"近邻失真，需要重嵌入重建","ok":true,"why":"同模型同维度是前提。"},{"t":"只需改数据库端口","ok":false,"why":"与端口无关。"}]},{"q":"多租户查询为何不能只靠「提示里写不要看别的租户」？","choices":[{"t":"提示不可靠；应在检索过滤 ACL","ok":true,"why":"安全边界在检索层。"},{"t":"因为 HTTP 不支持 JSON","ok":false,"why":"无关。"},{"t":"因为向量没有维度","ok":false,"why":"向量有维度。"}]}]}
\`\`\`

## 下一步

**混合检索**（关键词补专名）· **重排序** · **RAG 评测**。
`;
