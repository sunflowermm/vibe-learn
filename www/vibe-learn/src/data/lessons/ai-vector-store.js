export default `# 向量库

> **本课位置**：嵌入之后的**存储与查询**层。  
> **先修**：Embedding 直觉、分块策略。  
> **要点**：向量库 = 近邻索引 + 元数据；换嵌入模型通常要整库重嵌。  
> **对照**：传统数据库按等值/范围查行；向量库按「离查询向量有多近」查邻居。  
> **下一课**：**混合检索** · **RAG 评测**。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 职责 | 存向量+元数据，支持近似检索 |
| 边界 | 向量库≠业务 DBMS 替代一切；ACL 在过滤层 |
| 本仓 | 小语料优先 tools.read；大库按产品能力 |
| 跟 Agent | 选型问维度、过滤、运维成本 |

\`\`\`algo
{"kind":"embnear","title":"同一嵌入空间 · 近邻 + 过滤前奏","autoplay":true,"speed":780}
\`\`\`

\`\`\`check
{"title":"向量库通关","items":[{"id":"ann","text":"能说明 ANN≈用少许召回损失换速度","hint":"索引"},{"id":"reemb","text":"知道换嵌入模型通常要重嵌重建","hint":"运维"},{"id":"acl","text":"多租户要靠 metadata 过滤，不靠提示自觉","hint":"安全"}]}
\`\`\`

## 定义

**向量库 / 向量索引**：存高维向量，按相似度返回近邻；常带 payload（文档 id、权限、时间、原文引用）。

| 概念 | 白话 |
|------|------|
| 维度 | 与嵌入模型绑定，同库须一致 |
| 度量 | 余弦 / 内积 / L2 等；建库与查询要一致 |
| ANN | 近似最近邻：用少许召回损失换速度 |
| HNSW 等 | 常见图索引；吃内存、查询快 |
| 过滤 | 租户 / ACL / 时间窗与近邻组合 |

## 和「普通数据库」怎么分工

| | 关系库 / 文档库 | 向量库 |
|--|-----------------|--------|
| 擅长 | 精确字段、事务、报表 | 语义相似 |
| 权限 | 行级/集合级成熟 | 必须显式做 metadata 过滤 |
| 变更 | 改一行即可 | 改嵌入模型 → 常要重嵌 |

实务：**元数据仍可放 PG/对象存储**；向量库存向量 + 回指 id。\`pgvector\` 则是「一库两用」。

\`\`\`mermaid
flowchart LR
  DOC[文档] --> CHUNK[分块]
  CHUNK --> EMB[同一嵌入模型]
  EMB --> VS[向量索引]
  Q[问题] --> EMB2[同一模型]
  EMB2 --> VS
  VS --> HITS[Top-K + 过滤]
  HITS --> LLM[拼进提示]
\`\`\`

## 选型直觉（不必背品牌）

| 需求 | 倾向 |
|------|------|
| 本地原型 | FAISS / Chroma / 内存索引 |
| 已有 Postgres | pgvector |
| 大规模多租户 | 专用向量服务 + 分区 / 过滤 |
| 强合规 | 自托管 + 加密与审计 |

## 本仓怎么做

| 概念 | 落点 |
|------|------|
| 小工作区知识 | 优先 \`tools.read\` / 工作区注入，不必默认上向量库 |
| 大库能力 | 按已加载的记忆/知识库/向量类 Core 与工作流接入 |
| 安全边界 | 可见范围在检索过滤做，不靠「提示里写别看别的租户」 |
| 真源 | \`docs/agent-context.md\`；向量/数据库 Core 见对应 README |

\`\`\`quiz
{"title":"向量库","questions":[{"q":"换了嵌入模型却不重建索引，通常会发生什么？","choices":[{"t":"自动对齐，无影响","ok":false,"why":"不同模型坐标空间不同，旧向量与新查询不可比。"},{"t":"近邻失真，需要重嵌入重建","ok":true,"why":"同模型同维度是近邻有意义的前提。"},{"t":"只需改数据库端口","ok":false,"why":"端口只影响连通，不修复嵌入空间错位。"},{"t":"只要把维度数字改成新模型的即可，旧向量可复用","ok":false,"why":"改元数据里的维度声明不会重算向量；旧坐标仍是旧模型的。"}]},{"q":"多租户查询为何不能只靠「提示里写不要看别的租户」？","choices":[{"t":"提示不可靠；应在检索过滤 ACL","ok":true,"why":"安全边界在检索/存储层；模型可能忽略提示或被注入绕过。"},{"t":"因为 HTTP 不支持 JSON","ok":false,"why":"HTTP 完全可以传 JSON；与租户隔离无关。"},{"t":"因为向量没有维度","ok":false,"why":"向量有固定维度；问题在权限控制面，不在有无维度。"},{"t":"因为向量库无法存储文档 id","ok":false,"why":"payload/元数据正是存文档 id 与租户字段的地方，应用来做过滤。"}]},{"q":"已有 Postgres 时，向量检索常见落地？","choices":[{"t":"可用 pgvector 一库两用；大规模多租户再考虑专用向量服务","ok":true,"why":"不必一上来独立向量集群。"},{"t":"Postgres 绝对不能存向量，必须另起专用向量集群","ok":false,"why":"pgvector 是常见路径。"},{"t":"有了 pgvector 就不必再做 ACL 与租户过滤","ok":false,"why":"过滤仍要显式做。"},{"t":"换嵌入模型只需改数据库端口，旧向量可直接复用","ok":false,"why":"要重嵌重建。"}]}]}
\`\`\`

## 下一步

**混合检索**（关键词补专名）· **重排序** · **RAG 评测**。
`;
