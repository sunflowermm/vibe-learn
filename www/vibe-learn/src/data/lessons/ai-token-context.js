/** 令牌 · 上下文窗口（计算段起点） */
export default `# 令牌与上下文窗口

> **本课位置**：学习路径**计算**段第 1 课。  
> **先修**：知道「大语言模型会读一段文字再往下写」即可。  
> **下一课**：注意力——进窗之后，各个令牌如何互相「看」。

## 先把三个词说清楚

| 中文全称 | 英文 / 常见写法 | 定义 |
|----------|-----------------|------|
| **令牌** | token | 模型读写的最小文本片。多为**子词**（不是「一个汉字 = 一个令牌」） |
| **上下文窗口** | context window | **一次请求**里，模型能同时处理的令牌上限（输入与输出常共享或分计额度） |
| **提示 / 消息列表** | prompt / messages | 你塞进窗口的内容：系统说明、历史对话、本轮用户话、工具结果、检索片段等 |
| **上下文工程** | context engineering | 在窗口预算内决定：让模型**看见什么、顺序如何、各块占多少** |

\`\`\`steps
{"title":"一次请求怎么吃预算","steps":[{"title":"切成令牌","body":"文本先被分词器切成令牌；中英比例不同，同样汉字数占用也不同。"},{"title":"占满窗口","body":"系统说明 + 历史 + 本轮 + 工具回传结果，都算进同一笔预算。"},{"title":"再生成","body":"模型写出的回答也占额度；超限会被截断或接口报错。"},{"title":"人工取舍","body":"限制历史条数、规则宜短、技能先只给目录——都是在抢窗口。"}]}
\`\`\`

---

## 常见现象 ←→ 窗口原因

| 你看到的现象 | 更可能的原因 |
|--------------|--------------|
| 「模型忘了刚才说的」 | 历史被截掉或根本没注入，那些字**不在窗口里** |
| 长篇常驻说明让回答变飘 | 常驻文本挤占了真正任务相关的上下文 |
| 要不要建向量库争论不休 | 同一预算下：外挂检索几段 vs 整段塞进提示 |
| 多轮调工具又慢又贵 | 每轮工具结果回灌，再次占用窗口 |

---

## 和邻近概念的边界（本课只点名，后文展开）

| 概念 | 和本课的差别 |
|------|----------------|
| **参数里的知识** | 写在模型权重里；单次对话改不了 |
| **本次上下文** | 只存在于这一次（或这一会话）请求的可见文本 |
| **键值缓存** | 英文 Key-Value Cache：推理时复用已算过的注意力中间结果，依赖**前缀稳定**（所以本仓把「易变时间」拆出系统层） |
| **嵌入向量** | 把文本变成数字向量做**检索相似度**；不替代「生成用的窗口」 |

\`\`\`mermaid
flowchart LR
  Tok[切成令牌] --> Win[上下文窗口]
  Win --> Sys[宜稳定的前缀]
  Win --> Dyn[历史 / 本轮 / 工具结果]
  Sys --> Cache[利于键值缓存]
  Dyn --> Task[完成本轮任务]
\`\`\`

---

## 本仓怎么做

| 行为 | 落点 |
|------|------|
| 限制注入总长、规则/技能/子代理预算 | \`ai-workflow.yaml\` → \`agentWorkspace\` 的 \`max*Chars\` 等 |
| 历史只留最近若干条 | 对话工作流里的会话笔录上限 |
| 技能先注入**目录**再按需读全文 | \`<available_skills>\` + \`tools.read\`（少占窗口） |
| 系统说明与「当前时间」分层 | \`assembleChatLlmMessages\`：易变层独立用户消息，见 **对话管线** · \`docs/agent-context.md\` |

\`\`\`quiz
{"title":"令牌与窗口","questions":[{"q":"上下文窗口主要限制什么？","choices":[{"t":"模型一共有多少参数","ok":false,"why":"参数量是模型规模，不是单次可见文本量。"},{"t":"单次请求里能同时处理多少令牌","ok":true,"why":"窗口 = 一次可见预算。"},{"t":"磁盘上有多少个技能文件夹","ok":false,"why":"文件多少不等于进窗多少。"}]}]}
\`\`\`

## 下一课

窗口里「哪个令牌该看哪些令牌」——**注意力**。
`;
