export default `# 重排序

> **本课位置**：召回之后、进提示之前。  
> **先修**：混合检索。  
> **要点**：召回求「别漏」；重排求「别噪」——窗口只能塞有限证据。  
> **下一课**：**向量库**运维直觉 · **RAG 评测**。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 位置 | 召回之后、进窗之前的精排 |
| 边界 | 重排≠再生成答案 |
| 成本 | 多一跳模型/服务，要算延迟 |
| 跟 Agent | 先有召回评测再谈是否上重排 |

\`\`\`algo
{"kind":"hybridret","title":"宽召回之后 · 融合/精排进窗","autoplay":true,"speed":760}
\`\`\`

\`\`\`check
{"title":"重排通关","items":[{"id":"role","text":"能说明重排从宽召回挑进窗 Top-N","hint":"职责"},{"id":"ce","text":"知道交叉编码器适合短候选列表","hint":"成本"},{"id":"skip","text":"能判断何时可先跳过再靠评测决定","hint":"取舍"}]}
\`\`\`

## 定义

**重排序（reranking）**：对召回的候选片段再打分，选出真正写入提示的 Top-N。

| 手法 | 说明 |
|------|------|
| 交叉编码器 | 问句+片段成对打分，准但更贵 |
| 轻量模型 / 规则 | 标题命中、权限、时效加权 |
| LLM 精排 | 贵，适合短候选列表 |

\`\`\`mermaid
flowchart LR
  Q[问题] --> R[宽召回]
  R --> RR[重排]
  RR --> CTX[写入上下文]
  CTX --> GEN[生成]
\`\`\`

## 何时几乎必须 / 可以先跳过

| 情况 | 倾向 |
|------|------|
| 混合召回 K 很大、噪声多 | 必须重排，否则「有据胡编」 |
| 候选已很少且字面强命中 | 可先跳过，用评测再决定 |
| 权限 / 时效强约束 | 规则重排或硬过滤优先于「语义好看」 |
| p95 延迟紧 | 缩小召回 K，或缓存热点查询后再排 |

## 和延迟/成本

| 杠杆 | 影响 |
|------|------|
| 召回 K 太大 | 重排变慢、变贵 |
| 跳过重排 | 噪声进窗，幻觉「有据」 |
| 缓存热点查询 | 降 p95 |
| 交叉编码器全量扫 | 准，但别把 K 开到几百 |

## 本仓怎么做

| 概念 | 落点 |
|------|------|
| 进窗名额有限 | \`assembleChatLlmMessages\` 三层 + \`agentWorkspace\` 预算：能塞的证据条数本身就是「硬 Top-N」 |
| 小语料 | \`tools.read\` 按路径精确取文件，相当于人工精排，不必先上交叉编码器 |
| 可观测 | 办事轨迹里看「实际塞进提示的材料」是否相关（见工程素养 · 可观测） |
| 真源 | \`docs/agent-context.md\` |

\`\`\`quiz
{"title":"重排","questions":[{"q":"重排的主要目标？","choices":[{"t":"把嵌入维度降下来","ok":false,"why":"维度由嵌入模型决定；重排只改候选排序，不改向量长度。"},{"t":"从宽召回里挑出最值得进窗的证据","ok":true,"why":"召回求全，重排求准；服务的是上下文预算。"},{"t":"替换生成模型","ok":false,"why":"重排在检索侧，不更换 LLM 权重。"},{"t":"把 BM25 与向量分数强制相加成同一个量纲","ok":false,"why":"那是融合/校准问题；重排是对已有候选再打分选 Top-N。"}]},{"q":"交叉编码器精排适合什么位置？","choices":[{"t":"短候选列表上成对打分求准；不适合对全库暴力扫","ok":true,"why":"宽召回用 ANN，精排在小集合上做。"},{"t":"替代生成模型写最终答案","ok":false,"why":"精排输出排序，不是最终回答。"},{"t":"专门降低嵌入维度","ok":false,"why":"做相关性打分，不是降维。"},{"t":"与 ACL 过滤互斥","ok":false,"why":"可先过滤再精排。"}]}]}
\`\`\`

## 下一步

**向量库**怎么存与查 · **RAG 评测**怎么证明变好。
`;
