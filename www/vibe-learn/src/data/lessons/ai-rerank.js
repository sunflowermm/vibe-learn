/** 重排序：精排进窗片段 */
export default `# 重排序

> **本课位置**：召回之后、进提示之前。  
> **先修**：混合检索。  
> **要点**：召回求「别漏」；重排求「别噪」——窗口只能塞有限证据。

## 定义

**重排序（reranking）**：对召回的候选片段再打分，选出真正写入提示的 Top-N。

| 手法 | 说明 |
|------|------|
| 交叉编码器 | 问句+片段成对打分，准但更贵 |
| 轻量模型 / 规则 | 标题命中、权限、时效加权 |
| LLM 精排 | 贵，适合短候选列表 |

\`\`\`mermaid
flowchart LR
  Q[问题] --> R[宽召回]
  R --> RR[重排]
  RR --> CTX[写入上下文]
  CTX --> GEN[生成]
\`\`\`

## 和延迟/成本

| 杠杆 | 影响 |
|------|------|
| 召回 K 太大 | 重排变慢、变贵 |
| 跳过重排 | 噪声进窗，幻觉「有据」 |
| 缓存热点查询 | 降 p95 |

\`\`\`quiz
{"title":"重排","questions":[{"q":"重排的主要目标？","choices":[{"t":"把嵌入维度降下来","ok":false,"why":"维度与重排无关。"},{"t":"从宽召回里挑出最值得进窗的证据","ok":true,"why":"精排服务上下文预算。"},{"t":"替换生成模型","ok":false,"why":"重排在检索侧。"}]}]}
\`\`\`

## 下一步

**向量库**怎么存与查 · **RAG 评测**怎么证明变好。
`;
