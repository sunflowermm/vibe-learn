export default `# 注意力

> **本课位置**：学习路径**计算**段。  
> **先修**：**令牌与上下文窗口**（窗外内容注意力也看不见）。  
> **下一课**：**变换器**——把注意力块堆成整机。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 定义 | 能口述自注意力：谁看谁、加权混合 |
| 边界 | 有注意力 ≠ 看见窗口外文本 |
| 衔接 | 服务令牌课；导向变换器 |
| 跟 Agent | 解释「模型在看什么」时回到窗口预算 |


## 定义（先写全称）

| 中文全称 | 英文 | 定义 |
|----------|------|------|
| **注意力** | attention | 根据「此刻要找什么」，给序列中每个位置打分，再按分数**加权混合**信息 |
| **自注意力** | self-attention | 查询、键、值都来自**同一条**输入序列 |
| **查询 / 键 / 值** | Query / Key / Value（常写作 Q / K / V） | 查询=要找什么；键=可被匹配的标签；值=匹配上后取出的内容 |
| **多头注意力** | Multi-Head Attention | 多组查询-键-值并行计算再拼接，便于学到不同关系 |
| **因果掩码** | causal mask | 生成时禁止看见「还没写出来的未来令牌」 |
| **键值缓存** | Key-Value Cache | 推理时缓存已算过的键与值，加速续写；依赖前缀少变 |

\`\`\`match
{"title":"查询·键·值","pairs":[{"id":"q","left":"查询 Query","right":"此刻要找什么"},{"id":"k","left":"键 Key","right":"每个位置可被匹配的标签"},{"id":"v","left":"值 Value","right":"匹配上后真正取出的内容"}]}
\`\`\`

---

## 计算在干什么（概念级，不要求会推公式）

对每个位置：

1. 把输入表示线性变换成 **查询、键、值**  
2. 用查询与各键做相似度打分（常见：缩放点积）  
3. 用 Softmax 把分数变成和为 1 的权重  
4. 用权重对 **值** 做加权和，得到新表示  

\`\`\`mermaid
flowchart LR
  X[输入表示] --> Q[查询]
  X --> K[键]
  X --> V[值]
  Q --> Score[打分并 Softmax]
  K --> Score
  Score --> Out[加权混合「值」]
  V --> Out
\`\`\`

直觉：一句话里每个令牌像会议室里的人；注意力 = 按「和议题相关程度」互相听，再把听到的话混成自己的新想法。  
它解决的是**序列内信息路由**，不是「自动记住你没放进窗口的文件」。

---

## 和「上下文窗口」的关系（必须连起来）

| 谁决定 | 做什么 |
|--------|--------|
| **你 / 框架** | 决定**谁进窗**（截断历史、规则宜短、技能按需读） |
| **注意力** | 在**已进窗**的令牌之间决定**谁看谁** |

- 理论上任意两位置可交互；实际上受窗口长度与大约平方级算力约束。  
- 「模型有注意力所以不会忘」是错的：窗外内容根本没有令牌可看。

\`\`\`steps
{"title":"读一句话时的四拍","steps":[{"title":"每个令牌一个位置","body":"序列里每个令牌对应一个向量。"},{"title":"互相打分","body":"代词「它」该看「小明」还是「桌子」——分数高者权重大。"},{"title":"混合","body":"按权重混合「值」，得到新表示。"},{"title":"堆层","body":"多层注意力 + 前馈网络 = 变换器积木（下一课）。"}]}
\`\`\`

---

## 本仓怎么做

本仓**不实现**注意力公式；调用的云端/本地大语言模型内部在做。  
你能做的是配合「键值缓存 / 前缀稳定」：

| 做法 | 原因 |
|------|------|
| 人设、协议、工作区说明放较稳定的系统层 | 前缀少变，利于缓存与稳定行为 |
| 当前时间、群角色等放独立用户消息 | 避免每分钟改系统层前缀 |
| 见 **对话管线** · \`docs/agent-context.md\` | 三层消息的工程契约 |

\`\`\`quiz
{"title":"注意力","questions":[{"q":"自注意力里查询、键、值通常来自？","choices":[{"t":"三个无关的外部数据库","ok":false,"why":"那是外挂检索，不是自注意力。"},{"t":"同一输入序列的不同投影","ok":true,"why":"「自」= 同源序列。"},{"t":"只来自系统说明、看不到用户话","ok":false,"why":"窗口内整段序列都参与（受掩码约束）。"}]}]}
\`\`\`

## 下一课

把注意力块堆成整机——**变换器（Transformer）**。
## 导图2 · Token / 上下文窗口 × 注意力

> 导图2 少直接写注意力公式；本课给应用直觉：窗内相关度加权。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **Token** | 被注意的单元 | 注意力在序列令牌之间 |
| **上下文窗口** | 可见范围 | 窗外内容零注意力——不是「懒」 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
