/** 第五章 · 人工智能（应用向概念图 · 对本仓对照） */
export default `# 第五章 · 人工智能

> **定位**：这是 **大语言模型应用与本仓对照** 的概念图，**不是**人工智能学位课表。  
> 刻意不展开：预训练目标与缩放律、系统评测专课、嵌入模型专课、对齐安全专课——缺了不等于「五步=学科全貌」。  
> **怎么读**：按下面路径的因果走；每课先给**中文全称**。旁支案例（如 Pi）**不进**主干先修。  
> 第四章已讲本仓怎么跑；本章补共用概念。真源：\`docs/agent-context.md\` · \`docs/agents.md\` · \`docs/ai-workflow.md\`。

## 和第四章怎么接

\`\`\`steps
{"title":"读前准备","steps":[{"title":"会指链路","body":"组上下文 → 调大语言模型 → 工具循环 → 回复。"},{"title":"再补词表","body":"按路径：计算 → 改参/接口 → 知识 → 行动 → 汇合 → 驯服。"},{"title":"每课回本仓","body":"看「本仓怎么做」，避免概念悬空。"}]}
\`\`\`

| 第四章已会 | 第五章补什么 |
|------------|----------------|
| 合并工作流、三层消息、工作区注入 | 为什么要限字符、拆 system / 易变层（窗口与缓存） |
| 大语言模型工厂、模型上下文协议运维 | 工具调用与插座标准、检索增强、规则与技能边界 |
| 办事助手工作区文件 | 规则 / 技能 / 子代理 / \`AGENTS.md\` 各自解决什么问题 |

---

## 缩写怎么读（全章约定）

| 写法 | 全称 |
|------|------|
| 大语言模型（Large Language Model，LLM） | 在海量文本上训练、能生成语言的神经网络 |
| 令牌（token） | 模型读写的最小文本片（常为子词） |
| 上下文窗口（context window） | 单次请求里模型能同时处理的令牌上限 |
| 自注意力（self-attention） | 同一序列内按相关性加权混合信息 |
| 变换器（Transformer） | 以自注意力 + 前馈层堆叠的主流骨干 |
| 上下文内学习（In-Context Learning，ICL） | 不改权重，只靠提示临时适配 |
| 微调（fine-tuning） | 在基座上继续训练以适配任务或领域 |
| 检索增强生成（Retrieval-Augmented Generation，RAG） | 先检索资料再生成（**不依赖**智能体） |
| 工具调用（tool calling） | 模型输出调哪个工具与参数，由运行时执行 |
| 模型上下文协议（Model Context Protocol，MCP） | 工具/资源的标准插接协议 |
| 智能体循环（agent loop） | 选行动 → 执行 → 观察 → 再决定 |
| 有向无环图（DAG） | 编排里表达步骤依赖与可并行关系的控制流图 |

---

## 学习路径（按因果，勿颠倒）

\`\`\`mermaid
flowchart TB
  S0[0 导读] --> S1[1 计算]
  S1 --> S2[2 改参与接口]
  S2 --> S3[3 知识]
  S3 --> S4[4 行动]
  S4 --> S5[5 汇合]
  S5 --> S6[6 驯服]
  S6 -.-> Side[旁支 Pi]
\`\`\`

| 段 | 课序 | 心智模型 |
|----|------|----------|
| **0 导读** | 学科诞生 → 规则到大模型 → 模型类型 | 人工智能 / 机器学习 / 大语言模型别混 |
| **1 计算** | 令牌与窗口 → 注意力 → 变换器 → 不止变换器（**选读**）→ 自适应总览 | 看见多少、谁看谁、骨干、适配分档 |
| **2 改参与接口** | 微调 → 对话产品化 → 会话补全接口 | 改权重一档讲细；消息列表事实标准 |
| **3 知识** | **检索增强生成**（经典流水线） | 知识外挂**先于**智能体；与自适应档位 D 对齐 |
| **4 行动** | 工具调用 → **模型上下文协议** → 协议分层 → **智能体与控制循环** → 智能体图编排 | 点菜语法 → 插座 → 循环 → 依赖图 |
| **5 汇合** | 智能体式检索 → 上下文工程 | RAG + 循环；窗内一切怎么拼 |
| **6 驯服** | 规则 → 技能 → 子代理 → 命令行 → \`AGENTS.md\` | 护栏与按需手册 |
| **旁支** | **Pi 脚手架**（挂命令行下，非先修） | 业界 harness 案例；对照不是升级课 |

### 反假依赖（必读）

- **经典检索增强不依赖智能体**；勿把 RAG 当成「Agent 的下一形态」。  
- **智能体的操作定义就是控制循环**；不必先空讲「形态」再讲「转圈」。  
- **模型上下文协议紧跟工具调用**；它不是「知识讲完才出现的检索协议」。  
- **Pi 是案例不是原语**；不会 Pi 照样学 RAG / 工具 / 循环。

---

## 本仓总对照（学完可回查）

| 概念 | 本仓落点（第四章 / 文档） |
|------|---------------------------|
| 上下文窗口与预算 | \`agentWorkspace\` 字符上限、历史条数、技能目录压缩 |
| 稳定前缀 / 易变层 | \`assembleChatLlmMessages\` 三层 |
| 上下文内学习 | 工作区 \`AGENTS.md\`、规则全文、技能**目录**、历史笔录 |
| 工具调用 | 大语言模型工厂 + \`registerMCPTool\`；禁止文本假函数 |
| 模型上下文协议 | 一等公民工具面；远程 \`remote-mcp.*\` |
| 智能体循环 | 工厂多轮 \`tool_calls\`；\`maxToolRounds\`；\`onAfterToolRound\` |
| 图编排（概念对照） | 主路径固定消息三层 + 工具环，非通用 DAG 编辑器 |
| 合并多工具面 | \`mergeWorkflows\` + 框架工具面 + 远程 MCP |
| 检索 / 记忆 | 记忆 / 知识库等工作流（按已加载能力） |
| 规则 vs 技能 | \`agents/rules/\` 全文；\`agents/skills/\` 目录 + \`tools.read\` |
| Pi（旁支对照） | 同为工具环；Pi 默认不内置 MCP——本仓相反 |

---

## 反模式

- 表格里只写缩写、不写中文全称  
- 用产品故事代替定义；把旁支案例塞进主干先修  
- 把规则写成小说、把技能当每次对话全量灌入  
- 以为「有注意力」就能看见窗口外的文本  

从 **令牌与窗口** 进入计算段（导读可先快扫）。
`;
