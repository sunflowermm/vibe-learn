export default `# 协议栈：OSI 与 TCP/IP

> 协议栈把「上网」拆成多层：每一层只解决一类问题。OSI 七层偏**教学地图**；TCP/IP 四层是**互联网实际在用的栈**。  
> **学会之后**：能对照 OSI 与 TCP/IP，并用五元组说明「谁和谁在通信」。

## 学会之后（验收）


\`\`\`sort
{"title":"自下而上排出协议栈","caption":"从上到下：靠近介质 → 靠近应用","items":[{"id":"phy","text":"物理 / 链路（网卡、帧）","order":0},{"id":"ip","text":"网络（IP 找主机）","order":1},{"id":"tr","text":"传输（TCP/UDP + 端口）","order":2},{"id":"app","text":"应用（HTTP、DNS…）","order":3}]}
\`\`\`


| 能力 | 成功信号 |
|------|----------|
| 分层 | 不把 L4 端口问题说成 L7 业务 bug |
| 封装 | 能口述应用数据如何套头向下发送 |
| 五元组 | 协议 + 源/目的 IP + 源/目的端口 |
| L4/L7 | 排障时知道先问传输层还是应用层 |
| 跟 Agent | 描述故障要带「卡在哪一层」而不是只贴截图 |

## 知识串

| 词 | 人话 |
|----|------|
| **协议** | 双方事先约定的对话规矩 |
| **协议栈** | 多层协议叠在一起，像寄快递的多级包装 |
| **OSI** | 七层教学参考模型 |
| **TCP/IP** | 互联网更常用的实用分层（常说四层） |
| **封装** | 发送时从上往下加「信封头」 |
| **解封装** | 接收时从下往上拆信封 |

上一课讲「有网」；本课讲「数据按哪几层规矩走」。下方实验室可点层看人话。

\`\`\`term
{"title":"从应用层看到的响应头（假）","prompt":"$ ","env":"本机（演示）","steps":[{"type":"in","text":"curl -sSI https://example.com | head -n 6"},{"type":"out","text":"HTTP/2 200\\ncontent-type: text/html; charset=UTF-8\\nserver: ECS (nyb/1D2A)\\n\\n# 应用层看到的是 HTTP；下面还有 TLS / TCP / IP（本窗不展开）"}]}
\`\`\`

## OSI 七层（自顶向下）

| 层 | 名称 | 典型内容 |
|----|------|----------|
| 7 | 应用层 | HTTP、FTP、SMTP、DNS… |
| 6 | 表示层 | 编码、压缩、加密语义 |
| 5 | 会话层 | 建立 / 管理 / 结束会话 |
| 4 | 传输层 | TCP、UDP |
| 3 | 网络层 | IP、路由 |
| 2 | 数据链路层 | MAC、帧、以太网 / Wi‑Fi |
| 1 | 物理层 | 电缆、光纤、无线电波 |

现代工程里，表示层、会话层的活常被应用库或 HTTP 自己「吞掉」，但对照概念时仍有用。

下方实验室可点每一层看人话，并「发送报文」看旅程。

## TCP/IP 四层（实用模型）

\`\`\`steps
{"title":"一次请求在栈里怎么走（示意）","caption":"教学简化：真实路径更曲折，但分层职责不变。","steps":[{"title":"应用层","body":"浏览器构造 HTTP 请求：方法、路径、头。"},{"title":"传输层","body":"TCP 分段、端口标识进程；必要时先握手。"},{"title":"网络层","body":"IP 负责寻址与路由：包要去哪台主机。"},{"title":"链路 / 物理","body":"帧与比特在网卡、交换机、介质上传递。"},{"title":"对端拆封","body":"自下而上解封装，最终回到应用层处理。"}]}
\`\`\`

\`\`\`quiz
{"title":"协议栈自测","questions":[{"q":"OSI 七层与 TCP/IP 四层的关系？","choices":[{"t":"必须一一对应，名字相同","ok":false,"why":"OSI 偏教学地图；工程上网常用 TCP/IP 四层，表示/会话常被应用层「吞掉」。"},{"t":"OSI 偏教学参考，TCP/IP 更贴近互联网实作","ok":true,"why":"对照概念用 OSI，排障与编程心智多用 TCP/IP。"},{"t":"只有物理层是真实的","ok":false,"why":"各层都有协议与实现，只是抽象粒度不同。"},{"t":"TCP/IP 已废除 IP，只剩 HTTP","ok":false,"why":"网络层 IP 路由仍是互联网核心。"}]},{"q":"五元组通常包含？","choices":[{"t":"源/目的 IP、源/目的端口、传输层协议","ok":true,"why":"标识一条 L4 流；安全组与连接跟踪常按它匹配。"},{"t":"仅 URL 与 Cookie","ok":false,"why":"那是 L7。"},{"t":"仅 MAC 与 VLAN","ok":false,"why":"链路层信息，不是五元组。"},{"t":"仅证书序列号","ok":false,"why":"TLS 字段，不是五元组。"}]}]}
\`\`\`


| 层 | 名称 | 典型协议 | 作用 |
|----|------|----------|------|
| 4 | 应用层 | HTTP、HTTPS、FTP、SMTP、DNS | 给应用程序提供网络服务 |
| 3 | 传输层 | TCP、UDP | 端到端传输；可靠或尽力而为 |
| 2 | 网络层 | IP、ICMP、ARP 相关 | 寻址与跨网转发 |
| 1 | 网络接口层 | 以太网、Wi‑Fi、PPP | 变成电/光/无线电信号发出去 |

### TCP/IP 对照 OSI

| | TCP/IP | OSI |
|--|--------|-----|
| 层数 | 4 | 7 |
| 取向 | 实用、互联网标准 | 理论、教学参考 |
| 应用侧 | 一层 ≈ OSI 应用+表示+会话 | 拆成三层 |
| 底下 | 网络接口层 ≈ OSI 链路+物理 | 链路、物理分开 |

## 封装与解封装（必懂）

发送时从上往下**封装**（加头）；接收时从下往上**解封装**（剥头）。

1. **应用层**生成数据（如一段 HTTP 请求）  
2. **传输层**加 TCP/UDP 头 → 段 / 数据报  
3. **网络层**加 IP 头 → IP 包（源/目的 IP、TTL…）  
4. **链路层**加帧头/尾 → 以太网帧（含 MAC）  
5. **物理层**变成比特流 \`010101…\`

接收端反过来拆。下层眼里，上层常常只是「一串字节」。

### 包里大概有什么

\`[ IP 头 | TCP 头 | 应用数据 ]\`

- IP 头：源/目的 IP、协议类型、TTL、校验等  
- TCP 头：源/目的端口、序号、确认号、标志位等  

## 三个现实例子

| 场景 | 应用层 | 传输层偏好 |
|------|--------|------------|
| 发邮件 | SMTP | TCP（要可靠） |
| 打开网页 | HTTP/HTTPS | TCP |
| 实时游戏 | 游戏自定义协议 | 常 UDP（要快，可忍丢） |

## 三个思想再钉一次

- **端到端**：两端对话，中间尽量只转发  
- **分层**：改一层不必重写整条链路  
- **封装**：下行加头，上行剥头  

## ICMP 与排障（补充）

**ICMP** 是网络层控制/诊断协议（不承载普通应用数据）。

| 工具 | 直觉 |
|------|------|
| **ping** | 探测主机是否可达、往返延迟 |
| **traceroute / tracert** | 观察沿途经过的路由器跳 |

能 ping 通不等于应用端口开放；应用问题仍需结合 TCP 端口与防火墙（见路由与 NAT）。

---

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **PDU（Protocol Data Unit，协议数据单元）** | 每一层给本层数据块起的名字，封装时会加上本层首部。 | 传文件时：应用层消息 → TCP 段 → IP 包 → 以太网帧 → 比特流；抓包工具按层解析不同 PDU。 | PDU 不是「一种固定格式」；各层名字不同：段/包/帧。 |
| **五元组（5-tuple）** | 源 IP、目的 IP、源端口、目的端口、传输层协议这五样合在一起唯一标识一条流。 | 云安全组、iptables、连接跟踪、会话保持都按五元组匹配；改 NAT 会改其中 IP/端口。 | 五元组 ≠ URL；L7 才看 Host/Path，L4 只看五元组。 |
| **TTL（Time To Live，生存时间）** | IP 头里的跳数计数，每经一个路由器减 1，减到 0 就丢弃，防止包在网络里无限循环。 | \`traceroute\` 故意发递增 TTL 的包，看每一跳谁回 ICMP；TTL 太小会导致「能 ping 部分跳但到不了目的地」。 | TTL ≠ DNS 缓存 TTL；DNS 的 TTL 是记录缓存多久，完全另一回事。 |
| **L4 负载均衡（Layer 4 Load Balancing）** | 只看 IP 和端口，把 TCP/UDP 连接转发到后端，不理解 HTTP 内容。 | 云 NLB、LVS 常用于数据库前置、游戏端口、TLS 透传；无法按 URL 路径分流。 | L4 LB ≠ 「更快」；只是看不到 HTTP 头，功能更窄。 |
| **L7 负载均衡（Layer 7 Load Balancing）** | 理解应用层协议，能按 Host、URL、Cookie、Header 等做路由与改写。 | Nginx/Caddy 反代 \`/api\` 到后端、\`/vibe-learn\` 到静态页；本仓多 Core \`www\` 挂载就是 L7 按路径分流直觉。 | L7 不是「只能 HTTP」；也可做 gRPC、WebSocket，但仍需解析应用语义。 |
| **封装与解封装（Encapsulation / Decapsulation）** | 发送时每层加自己的头尾；接收时自底向上逐层剥头交给上层。 | VPN 在 IP 包外再包一层加密头，就是额外封装；抓包看到「奇怪的外层协议」常由此而来。 | 封装 ≠ 压缩；只是加协议头，不一定减小体积。 |
| **OSI 七层 vs TCP/IP 四层** | OSI 是教学用的七层模型；TCP/IP 是互联网实际常用的四层（链路/网际/传输/应用）。 | 面试爱问「HTTPS 在第几层」——HTTP 在应用层，TLS 可视为表示/会话语义，跑在 TCP 之上。 | OSI 层号与 TCP/IP 不是一一对应；别死记「第几层」而不说具体协议。 |
| **HTTPS 在哪一层** | HTTP 定义请求/响应语义；TLS 在其下提供加密与身份验证，两者合称 HTTPS。 | 证书过期、SNI 不匹配、链不完整都是「应用侧安全」问题，换网线或重启路由器解决不了。 | HTTPS ≠ 「网络层加密 IP」；IPsec 才是另一种网络层安全方案。 |

排障口诀：先定层——DNS/证书/HTTP 状态码偏上；ping/路由/防火墙偏下。

## 下一步

**IP 与子网** · **TCP / UDP**。
## 导图2 · HTTP / HTTPS / DNS × 协议栈分层

> 导图2 词条多落在 **应用层口语**；本课钉 **OSI/TCP/IP 地图与五元组**。  
> **排障分层以本课为准**：先定位层，再查 HTTP/DNS 细节专课。

| 导图2 | Vibe 口语 | 本课专业落点 |
|-------|-----------|--------------|
| **HTTP** | 应用层说话 | 多在 L7；底下仍要 IP/端口/TCP |
| **HTTPS** | 加密的 HTTP | TLS 常画在应用/表示附近；仍依赖可靠传输 |
| **DNS** | 名字解析 | 应用常用 UDP/TCP 53；别当成「第七层专属魔法」 |
短表只对齐口语；定义走面板「跨导图」或自动附录。验收与禁区仍以本课为准。
`;
