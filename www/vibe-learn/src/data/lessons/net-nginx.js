/** Nginx（网络章 · 反代产品课） */
export default `# Nginx

> **Nginx**（读作 engine-x）：高人气的 **Web 服务器 / 反向代理**。  
> 常干三件事：**挂静态文件**、**把请求转给后面的应用**、**在入口做 TLS（HTTPS）**。  
> **概念总论**是上一张 **反向代理与 CDN**；本课钉 **产品直觉**。  
> 和 **Docker** 可以组合用，但 Docker 在番外 **容器**——先分清「门面」与「打包」。

## 本课你要带走什么

1. Nginx 属于哪一层（门面，不是业务框架、不是容器引擎）  
2. 和 Node 本仓开发态怎么对照  
3. 和 Caddy / Traefik / 云 LB 的粗对照  

---

## 1. 它是什么

| 点 | 说明 |
|----|------|
| **分类** | Web 服务器 / 反向代理（工程上也常归「网关」） |
| **典型端口** | 80（HTTP）、443（HTTPS） |
| **配置** | \`nginx.conf\`、\`server\` / \`location\` / \`proxy_pass\` |
| **不是** | 编程语言；不是 Docker；不替代 Express 写业务 |

\`\`\`mermaid
flowchart LR
  U[浏览器] --> Nx[Nginx]
  Nx -->|静态| FS[html/css/js]
  Nx -->|反代| App[Node / 其它上游]
\`\`\`

本仓开发期常是 **Node 自己挂 www**；生产外层再加 Nginx / 云 LB / CDN 很常见。

---

## 2. 和容器的组合（可选，后学）

学完番外 **容器** 后再看这张表即可：

| 形态 | 直觉 |
|------|------|
| 宿主机装 Nginx | apt / brew；改配置后 reload |
| Nginx 也进容器 | Compose 里一个 \`nginx\` service，反代到 \`app:3000\` |
| 只要云厂商 LB | 可能看不到自建 Nginx，**角色仍是门面** |

---

## 3. 同类产品粗对照

| 产品 | 一句话 |
|------|--------|
| **Nginx** | 老牌、配置灵活、资料极多 |
| **Caddy** | 自动 HTTPS 体验友好 |
| **Traefik** | 动态路由、容器友好 |
| **Apache httpd** | 历史悠久，存量大 |
| **云 ALB / CDN** | 托管型门面 + 全球边缘 |

## 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **反向代理（Reverse Proxy）** | 靠近服务器侧的门面，替后端收请求再转发 | Nginx \`proxy_pass\` 到 Node | 别和 Clash **正向代理**混；也别和 Docker 混 |
| **TLS 终止（TLS Termination）** | 在门面卸下 HTTPS，后面可用 HTTP | 证书装在 Nginx | 终止 ≠ 透传 |
| **静态资源（Static Assets）** | 不经应用逻辑直接返回的文件 | \`www\` 构建产物 | 静态 ≠ API |
| **upstream** | Nginx 里一组后端服务器 | 多实例负载 | 不是容器专用词 |

## 下一步

**边缘与出口实务** — CDN/回源/出口（课内点回即可）；  
本仓 **HTTP 与 www** — 开发期挂载与生产门面对照（图谱主桥）；  
需要打包中间件时 → 番外 **容器**（组合用法，另一层）。  
`;
