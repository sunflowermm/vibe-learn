export default `# 第一个程序

> 编程不是背框架名，是闭环：**写意图 → 存成文件 → 用运行时执行 → 看输出/报错**。  
> 本仓主服语言是 **JavaScript**，本课运行时是第一章装好的 **Node.js**（\`node\`）。

## 学会之后（验收）

| 能力 | 成功信号 |
|------|----------|
| 跑通 | 自建 \`hello.js\`，\`node hello.js\` 打印预期一行 |
| 心智 | 知道源码是文本文件，被运行时加载进进程 |
| 排障 | \`command not found\` / 路径错时，先回第一章 PATH / \`pwd\` |
| 跟 Agent | 教练模式：先提示再评审，不要只要最终答案 |

本章框地图见 **第一章半 · 编程基础**。下一课：**值与类型**。

---

## 先认词

| 写法 | 白话 |
|------|------|
| **源码** | 你写的 \`.js\` 文本文件 |
| **运行时 / Runtime** | 读源码并执行的程序；本课是 Node |
| **终端** | 敲 \`node hello.js\` 的窗口（与第一章「终端 / Shell」同一层） |
| **REPL** | 交互试一句（\`node\` 回车）；**项目以文件为准** |
| **console.log** | 把文字打印到终端（也是一种「输出」） |

---

## 1. 先看成功长什么样（模拟）

> 沙箱假执行：只识别简单 \`console.log\`。真项目请在本机建文件。

\`\`\`shell
{"preset":"node-hello"}
\`\`\`

\`\`\`check
{"title":"第一程序通关","items":[{"id":"file","text":"本机有 hello.js 文本文件"},{"id":"run","text":"node hello.js 打印预期一行"},{"id":"err","text":"故意写错一次，能按报错找到行号"}]}
\`\`\`

跟一遍芯片：\`cat hello.js\` → \`node hello.js\`，应看到 \`hello, xrk\`。

---

## 2. 本机真做（推荐）

1. 用编辑器打开**仓库外练习目录或临时文件夹**（不必在本仓根里练第一行）  
2. 新建 \`hello.js\`，写入并**保存**：

\`\`\`javascript
console.log('hello, xrk')
\`\`\`

3. 在该文件所在目录打开终端，执行：

\`\`\`bash
node hello.js
\`\`\`

应打印一行 \`hello, xrk\`。

可选：敲 \`node\` 进 REPL，试 \`1 + 1\`、\`console.log('hi')\`，\`.exit\` 离开。  
**可复现的项目代码永远以文件 + \`node 文件\` 为准。**

---

## 3. 失败时先查哪（连回第一章）

| 现象 | 先查 | 回哪一课 |
|------|------|----------|
| \`node: command not found\` | 没装 Node，或 PATH 未刷新 | **运行时 · Node** / **安装器与 PATH**（新开终端） |
| \`Cannot find module\` / 找不到文件 | 当前目录错、文件名拼写错 | **Linux 基础指令**（\`pwd\` / \`ls\`） |
| 没输出也没报错 | 是否保存；是否跑错路径 | **工作台 · 编辑器**（先保存再跑） |
| 语法红字 | 看报错里的**文件:行号** | 后面的 **读懂报错** |

你刚完成的是「发令 → 进程执行一段脚本」。后面每一课都在这个闭环上加砖。

## 下一步

**值与类型** — 程序里那些 \`42\`、\`'xrk'\`、\`true\` 各是什么。
`;
