/** HTTPS 与 DNS — 对齐课件 */
export default `# HTTPS 与 DNS

> 打开一个网址时，你其实做了两件大事：  
> **DNS** 把名字变成 IP；**HTTPS** 在加密通道里说 HTTP。

## 知识串（接在哪）

| 已学 | 本课 |
|------|------|
| **IP** 找主机 | DNS：人记域名 → 机器用 IP |
| **TCP** 可靠连接 | HTTPS：在 TCP 上再套 TLS |
| **HTTP** 请求/响应 | HTTPS：加密后的 HTTP |

记忆钩：**先问清门牌（DNS），再加密说话（HTTPS）。**

---

## 1. HTTPS：加密的 HTTP

| | HTTP | HTTPS |
|--|------|-------|
| 传输 | 常为明文 | 经 **TLS**（旧称 SSL）加密 |
| 风险 | 易被窃听、篡改 | 机密性、完整性更好 |
| 地址 | \`http://\` | \`https://\`（常伴小锁） |

入门记法：**HTTPS ≈ HTTP + TLS（加密通道）**。

### TLS 握手在干什么（简化）

1. 客户端告知自己支持的加密能力  
2. 服务器选定算法，并发送**证书**  
3. 客户端验证证书（是不是可信机构签的、域名对不对等）  
4. 双方商定会话密钥  
5. 之后用密钥加密通信  

| 词 | 人话 |
|----|------|
| **证书** | 网站的「身份证」，由证书机构签发 |
| **TLS** | 负责加密与身份校验的协议层 |
| **小锁** | 浏览器认为证书与加密大致正常（不等于网站绝对可信） |

证书解决的是：「我是不是在和我想的那台服务器说话」，而不只是「通道有加密」。

---

## 2. DNS：互联网的电话簿

> **DNS（Domain Name System）** 把好记的**域名**（\`www.example.com\`）翻译成机器用的**IP**。

### 为什么需要 DNS？

人记名字容易，路由器认 IP。没有 DNS，你得手写一串数字才能上网。

### 解析大概怎么走

\`\`\`mermaid
sequenceDiagram
  participant U as "你/浏览器"
  participant C as 本地缓存
  participant D as "本地 DNS"
  participant A as 权威服务器
  U->>C: 查 example.com
  alt 缓存命中
    C-->>U: IP
  else 未命中
    U->>D: 询问
    D->>A: 逐级查询
    A-->>D: IP
    D-->>U: IP（并缓存）
  end
\`\`\`

1. 输入 \`www.example.com\`  
2. 先查浏览器 / 系统**本地缓存**  
3. 没有则问本地 DNS（常是运营商或你配的 8.8.8.8 等）  
4. 本地 DNS 再去问：根 → 顶级域（如 \`.com\`）→ 权威服务器…  
5. 得到 IP，返回给你，并常会缓存一段时间  

### 域名层次

| 层级 | 例子 | 谁在管（直觉） |
|------|------|----------------|
| 根 | \`.\` | 全球根服务器体系 |
| 顶级域 TLD | \`.com\` \`.cn\` \`.org\` | 相应管理机构 |
| 二级域 | \`example.com\` | 注册后由你/服务商管 |
| 子域 | \`www.\` \`mail.\` | 域名所有者配置 |

### 常见记录类型

| 类型 | 作用 |
|------|------|
| **A** | 域名 → IPv4 |
| **AAAA** | 域名 → IPv6 |
| **CNAME** | 别名 → 另一个域名 |
| **MX** | 邮件服务器指向 |

### 托管 DNS 与「解析对不对」

许多站点把 **NS** 指到 Cloudflare 等托管 DNS：改记录、TTL、是否「代理到边缘」都在控制台完成。  
排障时先分清：**权威给的 IP 是什么**，以及本机/运营商解析器是否返回了**不同结果**（缓存过期、错误解析器、或路径干扰导致的异常解析）。

| 现象 | 方向 |
|------|------|
| 权威正确，本地错误 | 清缓存、换可信解析器对比 |
| 解析到 CDN/CF 地址 | 正常：橙云代理时常如此 |
| 能解析但连不上 | 多半不是 DNS，而是路由/防火墙/过滤（见边缘实务） |

---

## 3. 打开 https://example.com 时实际顺序

**DNS 得到 IP → TCP 连接 → TLS 握手 → 再发 HTTP 请求**。

| 失败点 | 你可能看到 |
|--------|------------|
| DNS 失败 | 「找不到服务器」 |
| 端口不通 | 超时、连接被拒绝 |
| 证书不对 | 浏览器安全警告 |
| HTTP 4xx/5xx | 页面或接口报错（那是应用层） |

---

## 4. 八股 × 业务串联

> 面试/自学常考名词。**缩写一律展开**；先懂白话再记英文。

| 名词（全称） | 白话（是什么） | 业务里长什么样 | 别和谁搞混 |
|--------------|----------------|----------------|------------|
| **递归 vs 迭代查询（Recursive vs Iterative DNS Query）** | 递归：解析器帮你一直问到权威直到有答案；迭代：上级只告诉你「下一步去问谁」。 | 电脑配置的 \`8.8.8.8\` 通常替你递归；根服务器对公众多为迭代，减轻负载。 | 递归/迭代是「谁负责继续问」；与 HTTP 重定向无关。 |
| **权威 DNS / 递归解析器（Authoritative / Recursive Resolver）** | 权威 DNS 持有域名官方记录；递归解析器是用户侧帮你查并缓存结果的 DNS。 | 在 Cloudflare/域名注册商改 A 记录改的是权威；用户「刷不到新 IP」多半是解析器缓存 TTL 未过期。 | 改 hosts 文件是本地强制，不是权威 DNS 也不是递归。 |
| **TTL（Time To Live，生存时间/缓存时长）** | DNS 记录被缓存多久，到期后客户端或解析器会重新查询。 | 切流、迁移 IP 前先把 TTL 从 3600 降到 60，减少「一半用户仍指向旧 IP」的时间窗。 | DNS TTL ≠ IP 头 TTL；IP TTL 是包跳数，完全两码事。 |
| **NS / SOA 记录（Name Server / Start of Authority）** | NS 声明这块域名由哪些 DNS 服务器负责；SOA 含主服务器、序列号、默认 TTL 等元信息。 | 把域名迁到 Cloudflare = 在注册商把 NS 改成 CF 提供的名字服务器；SOA 序列号变大表示区域文件有更新。 | NS 记录 ≠ A 记录；NS 指「谁管」，A 指「IP 是多少」。 |
| **DNS 污染 / 劫持（DNS Poisoning / Hijacking）** | 解析路径上某环节返回错误或伪造 IP，使用户连到非预期服务器。 | 权威 DNS 正确、本地解析错，对比 \`dig @8.8.8.8\` 与运营商 DNS；Clash 等也会改本地 DNS 行为。 | 污染是「答案错了」；链路中断是「根本问不到」，症状不同。 |
| **证书链 / CA（Certificate Chain / Certificate Authority）** | 浏览器信任「根 CA → 中间 CA → 站点叶证书」这条链，证明公钥属于该域名。 | 漏配中间证书会导致部分浏览器报 ERR_CERT_AUTHORITY_INVALID；Let's Encrypt 需配 fullchain。 | 自签证书不是「链不完整」；是自签根本不在系统信任库里。 |
| **SNI（Server Name Indication，服务器名称指示）** | TLS 握手时客户端明文带上要访问的域名，让同 IP 上多站点服务器选正确证书。 | 一台 Nginx 上 \`a.com\` 与 \`b.com\` 共用 443，靠 SNI 选不同 \`ssl_certificate\`；老客户端无 SNI 会配默认证。 | SNI 是 TLS 扩展；HTTP 的 \`Host\` 头在加密层之内，顺序上 SNI 更早。 |
| **HSTS（HTTP Strict Transport Security，严格传输安全）** | 服务器通过响应头告诉浏览器「以后只许用 HTTPS 访问我，别尝试 HTTP」。 | 防 SSL 剥离降级攻击；一旦开启 \`max-age\` 很大，配错证书会长期难用 HTTP 回退调试。 | HSTS ≠ HTTPS 本身；它是浏览器侧的强制策略，要 HTTPS 已能正常用再开。 |
| **对称 / 非对称加密（Symmetric / Asymmetric Encryption）** | 对称：加解密同一密钥，快，适合传数据；非对称：公钥加密私钥解，适合交换密钥与验身份。 | TLS 握手用非对称（或 ECDHE）协商会话密钥，传 HTTP 正文用对称 AES-GCM；抓包只能看到密文。 | 「HTTPS 加密」不是全程非对称；那样太慢，实际混合使用。 |

业务串联：上线「只改了代码证书却报警」→ 查 DNS 是否指到新入口、SNI/证书是否匹配、回源是否要求 HTTPS。

## 下一步

**HTTP 与 Web** 看请求方法与状态码；**反向代理** 常在入口做 TLS 终止；**边缘实务** 把 DNS 污染症状、CF 橙云与出口对照起来；番外 **Clash** 也会插手 DNS/选路。
`;
