/** TypeScript 动手入门 */
export default "# TypeScript 动手入门\n\n> **TypeScript（TS）** = JavaScript + 类型标注；编译后仍是 JS。本仓主服以 **JS** 为准，但读 TS 示例、写更稳的工具函数时用得上。  \n> 观光卡见第二章 **TypeScript**；本课只练最小动手。\n\n## 最小对照\n\n```typescript\nfunction add(a: number, b: number): number {\n  return a + b\n}\nconst name: string = 'xrk'\n```\n\n| JS | TS 多了什么 |\n|----|-------------|\n| 运行时才爆 | 编辑器/编译期先拦一批错 |\n| 参数任意 | 可标注类型、接口、`type` |\n\n```flip\n{\"title\":\"TS 翻卡\",\"cards\":[{\"front\":\"类型标注\",\"back\":\"给编译器看的约束，运行时通常擦除\"},{\"front\":\"any\",\"back\":\"放弃检查；能少则少\"},{\"front\":\"与本仓\",\"back\":\"主服契约是 JS；TS 需编译进 JS\"}]}\n```\n\n## 建议练习\n\n1. 用 `tsc` 或在线 Playground 跑通上面 `add`  \n2. 给对象写一个 `interface User { id: number; name: string }`  \n3. 故意传错类型，看报错  \n\n## 下一步\n\n回 **语言版图 · TypeScript** 或继续 **最小贡献路径**（写插件仍可用纯 JS）。\n";
